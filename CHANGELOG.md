# Changelog

Nestboard 的所有重要变更都记录在此文件。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

---

## [未发布]（`2.1.4` 候选）

> 完整的 13 项清单、逐项设计与进度见 `11-2.1.4开发计划.md`（落地记录在它的 §8）。
> 本文件在 `2.1.4` 发版时统一补全，先记已经进包的那几件。

- **卡片外观档：原版 / 拟物**（`F2` 第一刀）：设置里多一行「卡片外观档」——
  「拟物」给卡片加上柔和的双阴影（**颜色跟随卡片自己的主色**，且**半透明** ——
  不透明的影子在点阵画布上会变成一块实色板/一圈硬边；大面（分栏）用小偏移 +
  大 blur —— 贴边那一圈全强度影子会像一道边框）、去掉描边、
  选中态换成一圈主色细环 + 轻微下沉；分栏同样处理（栏头跟着栏收上圆角、摘掉 1px 硬底线，栏体那个"槽"
  四个角都圆、往内收 1px、垫一层极淡的底色；凹陷只画**上沿**那道由亮入暗的暗带，
  槽底一个笔触都不留 —— 白底上任何"底边的唇"自己都会变成能看见的一条），
  深色主题另调一套参数。
  工具栏 / 过滤条 / 演示条 / 链接总览 / 面包屑等浮动面板同样凸起；搜索 / 过滤 / 评论框
  与音频进度轨道凹进去（卡片正文编辑区刻意不凹 —— 它铺满整张卡）。右键菜单走
  Obsidian 的 Menu，暂不套拟物（越界嫌疑，单独评估）。
  「原版」一个字都没动：整套拟物规则都挂在一个类下面，不写类 = 完全没这回事
  （所以"原版与 2.1.3 逐像素一致"是结构上成立的）。
  （初版给"内容槽"也加了一段内阴影，手工验收时发现它让整张卡读成了"凹进去"、
  而且因为内容槽没有圆角、卡片底部会露出方形角 —— 已去掉，改为整卡只做凸起。）
- **可以复制/剪切/粘贴**脑图里的**几个节点**了：框住它们 → `⌘C` → 在空白处 `⌘V`
  ⇒ 落成一棵**新树**（以其中一个节点为根，子树形状与顺序不变；节点之间的线也跟着过去）；
  `⌘X` 则源节点删掉（`⌘Z` 可撤销）。★ 文件树（`.nestmind`）里的节点暂不支持。
- 内部：补了一条**千节点规模**的回归用例（屏幕外的树不挂 DOM，但几何与节点表一个都不少）。
- **「从模板新建白板」那个弹窗收拾了一遍**：很扁的缩略图不再躺在纯灰盒里像"图没画满"
  （预览盒改成点阵底 + 缩略图带白纸与浅投影）；弹窗不再出现两条滚动条；
  矮窗口下也不会顶出屏幕。
- **画布过滤条挪到了右下角**（不再压住画布左侧的操作区）；画布**右键菜单**里多了
  「过滤卡片…」，只读板上也能用（它只是换一种看法，不改内容）。
- **连线的端点可以重拖了**：单选一条线 → 两端浮出小圆点，拖它就能改绑到别的卡片 /
  分栏 / 脑图节点上，拖到空白则变成自由端（从前拖空了只能删掉重画）；
  同时卡片**被选中**时那个"收起连线"的小把手也会显示（不再只 hover 才出现）；
  空板提示改成"加一张卡片或一棵脑图"。
- **第一轮验收修掉的一批**：选中一棵树 / 几个节点按 `Delete` 现在真的会删（从前毫无反应）；
  树身的右键菜单（「加入演示 / 移出演示 / 前移 / 后移」）现在弹得出来，根节点右键也有；
  命令面板里的「加入演示 / 移出演示」与 `⌘⇧↑` / `⌘⇧↓` 在"只选中一棵树"时不再消失；
  **新建脑图**的节点带默认文字（「中心主题」+「分支主题 N」）；**导出 `.nestmind`**
  按根节点文字命名（空则"未命名"，重名顺延）；空板上只放一棵树时不再提示"这块白板还是空的"；
  收起连线的小把手放大加投影，好认多了。
- **框住几个节点就只选那几个**：框选一棵树的一部分 → 只选中那几个节点（`Delete` 只删它们，
  指向它们的线一起走）；框住整棵 / 只擦过树的空白 → 仍是"整棵"。同时 `⌘⇧↑` / `⌘⇧↓`
  现在对**整棵树**生效（卡片与脑图的层级在同一格里排）。
- **引用一块"只有脑图"的白板**不再显示成空板：概要行会写"N 张卡片 · M 棵脑图"。
- **老卡类型收口**：画布过滤条不再列出"脑图 / 脑图卡"两个永远筛不出东西的开关；
  ★ 修掉一处真缺陷 —— 把 `.nestmind` 改名之后，白板上引用它的那棵树**现在会跟着
  改路径**（此前会突然报"找不到这份脑图文件"）；内嵌树里的附件与内联链接同样跟随。
- **演示里讲一棵脑图：落点终于对了**（修）：相机要的是**世界坐标**，而脑图那一条从
  `b93` 起递进去的是**屏幕坐标**（乘了 zoom、加了平移）⇒ 同一个步骤从不同地方翻过去，
  停的位置都不一样。现在直接给世界矩形（与卡片那条同一套单位），并且仍然"看到全脑图"。
- **删掉一棵脑图之后 `⌘Z` 能把整棵树找回来了**（修）：撤销快照里从前没有 `minds` 这一格
  （`2.2.0` 加脑图时漏的）⇒ 删树撤不回来，加树撤销也不会消失。现在快照把脑图一起带上
  （连内嵌模型），并且空列表不留下 `"minds": []` 这种空壳键。
- **演示里讲一棵脑图的取景口径定了**（修）：**看到全脑图**（框整棵树）+ **不飘**
  （几何取自**纯布局**，原点就是根节点中心）—— 上一版只框根节点，整棵树大半在视口外。
- **白板上的脑图也能用键盘了**：点一个节点之后 `Tab` 加子节点（直接进编辑器）、
  回车加同级、`Shift+Tab` 提升一层、`F2` 改名、`Delete` 删掉它、四个方向键在节点间换选中 ——
  与 `.nestmind` 视图**同一张键位表**（只接线，不抄一份）。
- **节点粘贴只能落在脑图节点上**（修）：从前复制脑图节点后，在白板空白处按 `⌘V` 会
  **凭空长出一棵棵树**（每个被复制的节点被包成了一棵新树）。现在节点走脑图自己的剪贴板，
  只在"指针下的那个节点"（或右键菜单里的「粘贴到这里」）落地，白板上别的落点**什么都不做**；
  顺带：卡片里复制的节点可以直接粘进 `.nestmind` 视图，反之亦然。
- **演示里讲一棵脑图不再"位置乱飘"**（修）：取景从"整棵树的外接框"改成**根节点**
  （那份整树框会随量测 / 折叠变，讲着讲着景就飘，树一大还得缩到看不清）。
- **同层等宽在画布上其实一直没生效**（修）：`min-width` 只抬高不压低，于是"这一层谁最宽"
  是拿**上一轮自己撑开**的宽度算出来的 ⇒ 下限被自己抹掉、节点缩回内容宽（看着就是
  "同一层长短不一"）。现在同层最宽一律由**内容真宽**算（量尺寸时先把下限摘掉），
  并且把它**落进几何** —— 连线 / 外接框 / 导出跟着一起对齐。
- **模板能带脑图了**：内置「研究」模板多了一棵「研究问题树」；「另存为模板」会把树上
  一起打包，「从模板新建」出来的板子也会带着它。★ 顺手修掉两处"静默丢东西"：
  沿用已久的**指向分栏 / 脑图的连线**在套用模板时会被丢掉（现在留住），
  以及内置模板此前**无法包含**脑图（工厂的 overrides 少一格）。
- **演示能讲脑图了**：一棵脑图现在可以作为演示的一步 —— 右键树（容器上）→
  「把这棵脑图加入演示」，演示到那一步时相机框住整棵树；「前移 / 后移一位」也在同一处。
  选中一棵树时，命令面板的「加入演示 / 移出演示」同样生效。
- **视口裁剪（白板级脑图）**：屏幕外的树不再挂着 DOM —— 一块放了几十棵树的板
  不会因为"看得见的只有两棵"而付全量的渲染代价。缩略图 / 导出取景与"指向屏幕外
  节点的连线"照旧正确（用的仍是同一份布局几何）。
- **拟物档：右键菜单与侧栏输入框** —— 我们自己的右键菜单无边框、走柔和双阴影，
  侧栏白板列表的筛选框凹进去。★ 只作用于**本插件开的**菜单（菜单上打了一个标记类），
  Obsidian 自己的「⋯」菜单与别的插件的菜单一个像素都不动。
- **侧栏「白板列表」的「按标签」现在认得卡内标签**：从前它只看白板级 `meta.tags` ——
  在便签正文里写的 `#纪要` 根本不进这一档（那一档会把它算成"未加标签"）。现在标签是
  **白板级 ∪ 卡内**的并集，点开一个标签还能看到它**写在哪几张卡上**，点那张卡直接跳过去。
  （Obsidian 的标签面板 / 全局搜索仍然看不到 `.nboard` 里的标签 —— 那是宿主只索引
  Markdown 的平台限制，插件侧无法打通，故走插件自己的界面。）
- **修：标签在全局搜索里搜不到** —— 索引笔记那个开关的说明从前只提了"链接"，
  用户不会知道**标签也依赖它**（不开的话白板里的 `#标签` 对 Obsidian 根本不存在，
  它从不读 `.nboard`）。说明已改写清楚，并写明"标签面板 / `tag:#标签` / 枢纽笔记"。
  另外：取标签从前**只扫便签卡**，待办卡等写的 `#标签` 进不了库 —— 现在标签走全部卡型
  （与过滤条同一份口径），链接仍只认便签卡。
- **标签进 Obsidian 体系**（`F1` 第一批）：卡内写的 `#标签` 现在会并进索引笔记的
  `tags:`（于是 `tag:#标签` 与标签面板能命中这块白板），并**每个标签生成一页枢纽笔记**
  （`<索引目录>/_tags/<标签>.md`，列出用到它的白板）。同样只在索引笔记开关打开时生成、
  同样只写带生成物标记的文件（用户占着同名路径就跳过）、「删除索引笔记」一并收干净。
- **连线可折叠（树化）**（`F7` 第一批）：每张卡右上角有一个**小方块把手**，拖到另一张卡上
  ⇒ 发起方成为**父级**，出现一条**中心到中心**的树连线（中段被卡片盖住，只露卡外的部分）。
  树规则：**只允许单父**、**成环直接拒绝**（弹提示）、已有父级不覆盖。右键菜单可
  「折叠子级（+N）」—— 子级整棵收起、卡上留一个可点击的 +N 角标（与"收起自己"是两个
  独立状态）—— 和「解除父子关系」。树连线与普通连线并存，`⌘Z` 每步可回滚。
- **修：分栏的 8 个缩放手柄从来没有样式**（陈年漏洞，拟物档垫底色后才现形）：手柄的
  基础规则只写了卡片（`.nestboard-card .nestboard-handle`），分栏的手柄因此不是绝对定位
  （在 flex 流里把内容槽顶出栏外 8–9px —— 选中分栏时能看到"凹陷超出选中框"）、也没有
  尺寸（基本点不中）。现在与卡片同款：选中分栏显示 8 个手柄、拖手柄缩放真的可用、
  内容槽回到栏内。原版档同样受益。
- **修：脑图节点变成竖排文字**（`O1` 的连带）：节点标题改成允许换行之后，
  节点元素（绝对定位、世界容器是 0×0 锚点）的宽度退化成"一个字"的 min-content ⇒
  文字一个字一行。给节点加上 `width: max-content`（宽度仍由内容算，仍不写死数字）。
  画布与卡内一并修好。
- **白板里的脑图卡**（`F3a`）：把一份 `.nestmind` 拖进白板，卡面**就是那份脑图本身** ——
  点节点即可就地改名、点圆圈折叠，右键节点还能加子节点 / 加同级 / 删除（改的都是那个文件，
  走 `.nestmind` 自己的原子写与冲突检测）。卡面只画**前 3 层**，更深的收成 `+N`；
  从**根节点**上按住拖 = 拖整张卡；双击卡的空白处在新标签打开它。
- **白板内建的脑图卡**（`F4`）：左侧工具栏 / 画布右键可以**直接在白板里建一张脑图**
  （中心主题 + 3 个分支），数据存在这张卡自己身上、不建文件，操作与上面那张完全一样；
  尺寸按内容算；右键节点（含中心主题）可以把这份脑图**导出为 `.nestmind`**。
  改动都进白板的撤销栈（`⌘Z` 能退）。
- **脑图节点手势的两处修正**（手工验收报的）：新建脑图卡之后**光标直接落在中心主题**上；
  右键「加子节点」之后**立刻就能打字**；改一个节点不会再让整张卡变空。
  另外**根节点现在也是一个普通节点**了 —— 点它选中、**双击它就地改名**、右键它给节点菜单；
  "从根上按住拖 = 拖整张卡"仍然保留，「打开脑图」改到**双击卡内空白**处。
- **脑图卡不再有那个"框"**（`F3a` / `F4` 两张都是）：边框、底色、阴影、内边距全去掉，
  画面上就是那棵脑图本身。卡片级的那些动作（**拖整张脑图** / 选中 / 右键菜单）现在都挂在
  **根节点**上：拖根节点 = 拖整张脑图，右键根节点 = 节点菜单 + 卡片菜单（颜色 / 锁定 /
  复制 / 删除…）合成一份。双击卡内空白仍然是「打开这份脑图」。
- **脑图卡也不再有那些"看不见的把手"**：调整大小的八个点、旋转手柄、四边中点浮出的连线锚点
  都不再出现（脑图不需要整卡改尺寸 / 旋转，也不作为整体对外连线）。于是它的操作点就只剩
  **节点自己** —— 点一个节点，画布底部会出现**这个节点**的快捷操作栏
  （标记 / 加粗 / 斜体 / 下划线 / 字色 / 文字高亮 / 节点底色），与脑图标签页里那条一模一样；
  改完直接落进这张卡（内嵌脑图卡进白板撤销栈，`.nestmind` 卡写回那个文件）。
- **脑图节点永远是原尺寸**：从前卡面装不下时会把整棵图**等比缩小**（于是"每加一个节点，
  所有节点都小一圈"）。现在反过来 —— **卡片跟着内容长大**，节点始终 1:1，与白板上其它卡片
  一样大；连续加节点也不会在历史里留下多余的记录（同一批尺寸变化合并成一步撤销）。
- **脑图从"卡里"搬到画布上**（重构第一步）：脑图不再是**一种卡片**，而是与卡片 / 分栏平级的
  **白板对象** —— 画面上就是一棵**没有边界**的树（连那个隐形的卡框都没有了），节点 1:1、
  想往哪个方向长都行、可以和任何卡片重叠混排。
  - 新建脑图（工具栏 / 画布右键）与把 `.nestmind` 拖进来，落下来的都是这棵树；
  - 点节点 → 画布底部出现**这个节点**的快捷操作栏；**拖根节点 = 挪整棵**；右键根节点出菜单
    （节点级 + 「删除整棵脑图」）；
  - **老板子里的脑图卡打开时自动变成这棵树**（连线不会断，因为沿用原来的 id）——
    而且**你不编辑那块板，文件就不会被改写**。
- **脑图的每个节点都能自己出连线**：把鼠标移到某个节点上，它的四边中点会浮出锚点 ——
  从那儿拖出线，可以落在**别的卡片 / 别的节点 / 空白处**（自由端）；反过来，
  从卡片拖过来的线也能**连到具体某一个节点**上（不是连整棵脑图）。
  - 线是**跟着节点走**的：挪整棵脑图 / 改名 / 折叠 / 展开，线都跟着；
  - 折叠收起来的那一支、以及被删掉的节点，它们的线不会画出来（删节点会把线一起删掉，`⌘Z` 可退）；
  - 那棵脑图**自己**仍然不作为整体对外连线 —— 连的永远是具体的节点。
- **脑图回到缩略图与导出里**：把脑图从"一张卡片"改成画布上的树之后，缩略图、PNG / PDF /
  SVG 导出这几处"只认卡片与分栏"的老代码里，那棵树会**整个消失**（一块只有脑图的板子甚至
  会说"没有内容可导"）。现在它们都补上了：缩略图里是那棵树的节点，导出图里是**同一份
  几何**（布局与连线路劲直接复用脑图那套），连到**具体节点**上的线也画得出来。
  已知缺口：笔记里嵌入的板 / 板列表小图里，指向 `.nestmind` 的树暂时不出现（内嵌的会）。
- **脑图节点里的字又能搜到了**（⌘F / 侧栏跨板搜索）：从前那套索引是照"一张脑图 = 一张
  卡片"写的，脑图变成画布上的树之后它就成了死代码 —— 搜节点里写过的词**一条都搜不到**。
  现在节点（连**备注**）都回到索引里，中心主题按"标题"那一档（它就是这棵树的名字）；
  点一条结果会**飞到那个节点并选中它**（底部那条快捷操作栏也跟着换过去）。
- **画布过滤也管到脑图**（过滤条里搜词）：从前过滤只认卡片，脑图一动不动 —— 现在按
  **节点**变淡（不匹配的那几个淡下去，匹配的那个亮着），于是"这棵树里哪一句在说这件事"
  一眼可见。两个刻意的口径：**只认文本维度**（节点没有类型、也没有链接，所以勾"只看待办 /
  只看断链"时脑图不变淡）、**模型读不到的那棵树不参与**（`.nestmind` 还没读回来时宁可不猜）。
  被过滤掉的节点仍然在模型里：它身上的连线还在，清空过滤 / `⌘Z` 之后原样回来。
- **整棵树进得了选区**（框选 / `⌘A` / `Delete` / 导出）：在空白处拖一个框**擦过**一棵树
  （哪怕只擦到一个节点），整棵就被选中 —— 画面上出现一圈与卡片 / 分栏**同色**的框；
  接着 `Delete` 会把它连同它身上的连线一起删掉（一步 `⌘Z` 回来），`⌘A` 也把它算进"全选"，
  导出对话框的「仅选中」会**只导这几棵树**。两处刻意的口径：判据是"外接框相交"
  （一棵铺得比屏幕还大的树也得框得中，不要求整棵都在框里），而**节点级的框选**还没有
  （框到节点 = 选中整棵树）。
  - `⌘D` **原地复制**整棵、`⌘C` / `⌘X` 把它写进系统剪贴板、`⌘V` 贴到任何一块板上去；
    内嵌脑图的**节点 id 会整份重编**（否则两棵树共用一批节点 id，指着节点的连线会串到
    另一棵树上），文件脑图则只复制容器（两份容器指向同一份 `.nestmind`）。
- **修：框住一棵树导出的 PNG 和原脑图差很多**（用户 2026-09-22 报）：导出 / 缩略图那一侧
  的"摆法"用的还是**默认字号 14** 在估尺寸，而画布上中心主题是 30px、一层 18px ——
  于是导出图里所有节点都被排成了 14px 的盒子（字却按层级画，顶出盒子）。现在估算**按层级**
  （字号 / 行高 / 折行上限都与样式表同一份数），并且标题带的高度、左对齐 + 内边距、
  粗体、圆角、长标题在节点内换行全部与画布对齐。**缩略图与 SVG 导出是同一个函数**，
  一起修好了。
- **修：导出图的尺寸与换行**（同一条报障的第二轮）：① **同层等宽**从前只是给样式表一个
  提示（画布靠"CSS 撑开 + 第二次测量"落地），导出侧没有第二遍 ⇒ 同层节点参差不齐、
  整体偏窄；现在布局**排两遍**把它算进几何。② 节点的**备注（内容块）整块没画过**
  （盒子留了高度、画面上空着）—— 现在画出来了，而且折行与布局估算共用同一个折行器。
  ③ 标题里的换行从前按 `\n` 断成多行（画布上其实是一行），多出来的行会溢出节点 ——
  现在两边都**折成空格**。④ 带备注的节点补上内容块自己的下内边距（8px）。
  顺带补齐：完成的节点整块变淡 + 标题删除线、附件那一行 `📎 文件名`。
- **导出补齐"节点内容"的最后两块**：**图片附件**现在会真的画出来（等比、居中、不拉伸，
  和画布上一样；图拿不到时画一块底纹而不是空着），第 4 层及更深的节点也照旧画文字与备注
  （画布上它们只是配色变透明）。至此"画布上节点里看得见的内容"在导出图上一样都不缺。
- **修：在指向文件的那棵脑图上右键「加子节点 / 加同级」之后，画面上不出现那个节点、
  光标也不进新节点**（把 `.nestmind` 拖进来或从文件建的那种；内嵌的那张一直是好的）。
  两个原因是同一条路上的两处遗漏：① 那份模型是**就地改**的（只把 `revision` 往上抬、
  对象还是同一个），而重画判据看的是"模型换没换对象" ⇒ 判成"没变"就不重画；
  ② "加完节点把光标送进去"的请求键，白板这一层按**脑图 id** 取，而菜单那一层写的是
  文件路径 ⇒ 请求永远没人取。现在前者按"对象 + 版本"判、后者由渲染方交出请求键。
- **便签卡与文档节点对齐**（`F5`）：便签的编辑态从"标题框 + 正文框两格"收成
  **只有正文一格**（与拖进来的 `.md` 文档节点同款），标题改走**卡面那一行的就地输入**
  —— 右键「编辑标题」，或者**双击卡面标题行**。两边"能改什么"本来就该一样：
  `⌘B`/`⌘I`、粘贴图片落库、`[[` 补全、列表 / 待办 / 引用 / 代码块的续行，全套都在这一格里。
- **引用卡（`.md` 文档节点）的摘要档改为渲染 Markdown**（`F5`）：从前摘要先把标记剥干净
  再当纯文本写进卡面，所以"预览"看着仍是一行行无差别文字；现在保留标记交给渲染器
  （`## 标题` 真的是标题、列表真的是列表），行数仍按 `excerptLines` 截。

## [2.1.3] — 2026-09-18

按社区目录的源码审核（`eslint-plugin-obsidianmd`）修掉三个 Error，并把 `minAppVersion` 提到 **1.8.7**：

- **`no-unsupported-api`**：代码里 10 处 `app.workspace.revealLeaf(...)` 用的是 Obsidian
  **1.7.2** 起才有的异步形式。不再回避：`minAppVersion` 1.5.0 → **1.8.7**（同时满足下面
  用到的 `getLanguage()`，它是 1.8.7 引入的）。
- **`no-static-styles-assignment`**：31 处 `el.style.xxx = '字面量'` 改成
  `el.setCssStyles({ xxx: '字面量' })` —— Obsidian 的官方写法，且 `setCssStyles` 没有
  `@since` 门控，任何版本都能用。
- **innerHTML**：脑图「大纲 / 树」切换器的图标不再 `seg.innerHTML = 常量SVG`，改由
  `DOMParser` 解析成节点再 `importNode`（`currentColor` 高亮照旧，弹出窗下也不会串 document）。
- **`prefer-get-language`**：宿主语言不再读 `window.localStorage.getItem('language')`，改成由
  `main.ts` 调用官方 `getLanguage()` 注入给 `util/i18n`（那个模块依然一个 obsidian 符号都不碰）。
- 其余提示（`prefer-create-el` 这种成规模的、`!important` / `:has` 这种刻意为之的、以及
  settings 声明式 API 的迁移建议）在提交的 PR 里逐条回复，不在本版动。

## [2.1.2] — 2026-09-18

按 Obsidian 社区目录审核给出的 CSS lint 建议收紧样式，**视觉与行为零变化**：

- **色板卡色号那组规则改用「提高选择器权重」**（作用域提到 `.nestboard-root`）而不是 `!important` ——
  审核明确要求优先这么写；`(0,3,0)` 已经压得住主题的 `button:focus` 那一档。
- **两处「两段式兜底」的 `background-color`**（脑图框选虚影 `.nestboard-mind-marquee`、
  缩略图导航器视口 `.nestboard-minimap__viewport`）改用 `@supports` 包裹现代写法，
  消除「同一条规则里重复声明」。
- 色号悬停的下划线由 `text-decoration` 简写改成 `text-decoration-line` +
  `text-decoration-style` 长写法。

其余 lint 提示为**刻意为之**、代码里都带注释，已在提交的 PR 里逐条回复：工具态光标用
`*` + `!important` 压住子元素（卡片是 `grab`、缩放手柄是 `nwse-resize`）；`:has` 用于卡片
状态样式（如白板卡迷你形态）；`clip-path` / `masks` / `scrollbar` 在 Obsidian 实际内核里可用；
行为类的「库枚举 / 剪贴板 / localStorage」分别是选文件选择器、复制粘贴、读宿主语言 —— 前两者是
插件本职，后者**只读不写**，插件数据全部走 `loadData` / `saveData`。

## [2.1.1] — 2026-09-18

- **改为仅桌面端**（`manifest.json` 的 `isDesktopOnly: true`）：移动端至今未实测。与其声明一个
  没验过的平台、给用户一个"装得上但可能不好用"的期望，不如先如实收窄。
- **不改任何功能与数据格式**：`.nboard` 与 2.1.0 完全兼容，升级无缝。
- **README 增补英文说明**：Obsidian 社区目录要求必须有英文描述（哪怕同时提供译文），
  故 README 顶部改为英文（简介 / 功能 / 安装 / 隐私 / 许可），中文全文保留在下方「中文文档」。
- 代码里仍然没有用任何桌面端专有 API（`eslint.config.mjs` 依旧禁用 Node 内置模块），所以将来
  移动端实测通过后，把这一位改回 `false` 即可放开，不需要动业务代码。

## [2.1.0] — 2026-09-18（GitHub 首发）

> 目标：**首个公开上架版**（Obsidian 社区插件市场）。完整方案与分期见 `10-2.1.0开发计划.md`。
> **用户手册**：`用户使用手册.md`（2.1.0 主手册，2026-09-18 重写）；`用户使用说明书.md`
> 是 2.0.1 时代的长文，两者冲突时以新手册为准。

### 批 3 · 跨卡操作与脑图（进行中）

- **卡片属性面板**（`B1`，进行中）：新增右侧边栏**单例**视图「卡片属性」
  （`ui/CardInspectorPanel.ts` + `VIEW_TYPE_CARD_INSPECTOR`），列出这张卡的
  **类型 / 卡片 ID / 所属位置（分栏）**（只读）+ **卡片名 / 颜色 / 显示标题栏 /
  显示边框与底色 / 锁定 / X / Y / 宽 / 高 / 旋转 / 层级**
  —— 每一行都用 Obsidian 的 `Setting`（外观与窄侧栏折行归宿主管，不自绘表单）。
  **面板不碰模型**：它通过三个窄接口向白板视图要数据、递改动，写回走视图的 `commit`
  ⇒ 每一次编辑都是**一步撤销**，与画布上的修改共享同一条历史；`locked` 的卡不改。
  **入口已接**：卡片右键菜单的**第一项**就是「**属性…**」（多选时置灰；视图没接这个能力时整项不出现）；
  打开后侧栏那个单例面板跟着这张卡，改任何一项都是**一步撤销**。
  **内容栏（先给摘要）**：面板里多一行「内容」——按类型给"这张卡里到底是什么"的一句话：
  便签 / 同步便签取正文首行、仅标题卡取那行字、链接取地址、文件类取路径，
  成组的（清单 / 色板 / 图集 / 手绘 / 评论）给"几项"而不是把内容摊开。
  **内容可以就地改了**：便签 / 同步便签的正文用**多行 textarea**、仅标题卡那行字 / 链接地址 /
  各种文件路径用单行、**色板**（一行一格色，含渐变）与**图集**（一行一个路径）用多行 ——
  解析不出来就**整批不提交**（色板那几行里只要有一行不是色号，与色板卡自己的编辑态同一条）。
  手绘 / 清单 / 评论的内容不是"一句话"，这一档仍只给只读摘要。

- **树视图面包屑**（用户 2026-09-18）：**点击修好了** —— 处理改挂在 `pointerdown` 上
  （画布在 `pointerdown` 里开始平移/框选并 `preventDefault()`，浏览器**不再派发 `click`**
  ⇒ 从前挂在 `click` 上的那一格永远收不到）；位置按反馈**向右 65px、向上 30px**
  （让开左上角那个「树视图 / 大纲视图」切换按钮），并显式 `pointer-events: auto`。
- **树视图也有面包屑**（用户 2026-09-18："脑图树视图，进入当前主题后，也要出现面包屑导航"）：
  画布左上角浮一条「根 › … › 现在这一支」，点前面任何一格回到那一层（与大纲那条、
  与 `⌘[` 是同一条路）。数据与**大纲同源**（`outlinePathOf`）—— 两边看到的路径永远是同一条；
  没聚焦时整条收起、在大纲视图里不画（那边有自己的一条）；每一格的样式与大纲**共用**。

- **把卡片拖进白板卡**（`B2`，用户 2026-09-18："在画布内把一张卡片拖动到一个白板卡上，
  相当于把这张卡片剪切进这个白板卡"）：拖着一张（或多张）卡，**松手时压在某张白板卡上**
  就把它们搬进那块板（`⌥` 拖 = **复制**，原卡留在原地）。
  判定用仓库现成的几何命中 `hitTest`（带旋转反算、`z` 最大者胜），测试点取**最上面那张
  被拖的卡的中心**；搬内容复用**跨板复制粘贴**那一套（引用、附件、尺寸归一都已经是它的活）；
  目标板只读 / 打不开就**什么都不动**（宁可这次没反应，也不搬一半）。
  **拖动时目标会亮**：手里这批压在哪张白板卡上，那张卡就亮一圈环（`is-drop-into`，
  用 `outline` 画的 —— 不占布局，且不会盖掉卡片自己的投影）；松手就收掉。
  高亮用的判据与落卡时**同一个函数** ⇒ 亮的那张与真正收到的那张永远是同一张。
  **进触发区时被拖的那批卡变半透明**（`is-drop-fading`，`opacity: 0.45`）、移开或松手即恢复
  （用户 2026-09-18："一旦进入触发区域，卡片会变半透明……移开触发区域，卡片恢复原先透明度"）。
  **`⌘Z` 一下回滚两边**：搬进目标板的那几个 id 会记成一条"同伴记录"，
  撤销源板那一步时若**那批卡真的回到了源板上**，就把目标板里那几个也删掉
  （判据是"真的回来了"而不是"猜这一下撤的是搬运"；只认栈顶那一条）。

- **空白处右键菜单补上新增的四张卡**（用户 2026-09-18："新增的卡片类型都要放到右键菜单里"）：
  「更多卡片 ▸」里现在还有 **标题卡 / 图集卡 / 视频卡 / 音频卡**（标签复用工具栏那一份文案）。
  ★ 视频 / 音频要先弹库内文件选择器，而"菜单一关会把模态框一起带走"——
  所以这两项**延到下一拍**再开选择器（菜单先收干净），它们才进得了右键菜单。

- **大纲进入 / 返回主题的过渡**（`D2`）：切换聚焦时行列表来一段 **160ms 的淡入 + 轻微下落**
  （只动 `opacity` / `transform`，几百行的列表也不掉帧；`prefers-reduced-motion` 下整段关掉）。
  ★ 连续换支（`⌘]` 连按）时同样每次都播 —— 靠"摘类 → 强制重排 → 加回"。
- **进入节点时那一行会"飞"到顶部**（`D2` 续，用户 2026-09-18："进入节点的动效还要增加
  节点文字向对应展开根节点飞行的动效"）：把被进入的那一行**复制一份幽灵**钉在它原来的位置，
  再 **240ms** 飞到顶部那个标题（`scale(0.85)` + 淡出）—— 眼睛跟着它走就明白"这一行成了新的根"。
  ★ 只在**往里走**时飞（判据：新聚焦的节点是原来那个的子孙）—— 往回退 / 跳到别的支时飞反而莫名其妙；
  ★ 飞的是克隆，不动真节点（真节点要跟着列表重排，参与动画会与列表那段过渡打架）；
  ★ `prefers-reduced-motion` 下不飞；动画被取消也会把幽灵收掉（不留残影）。

- **树视图也支持「进入当前主题」**（`D1`）：`⌘]` 进入所选这一支、`⌘[` 返回上一层 ——
  与**大纲**里那两键完全一致（从前只有大纲接了，同样的两键在树视图里没反应）；
  节点右键菜单最前面也补了「进入这一支 / 返回上一层」（只在够用时出现）。
  **画布真的会"走进去"**：布局新增 `focusId`，把那一支**当作新的根重排** ——
  节点、连线、自动取景、缩略图全都只画这一支；同时**跳过悬浮节点那一趟**
  （不跳的话，中心主题与别的分支会以"悬浮节点"的身份继续杵在画面上，
  走进一支之后旁边还站着一堆不相干的东西）。聚焦的 id 不在了就退回整棵树。

### 批 2 · 新卡类型（四张全部落地）

- **仅标题卡按反馈改版**（`A3` 二轮，用户 2026-09-18）：
  - **只保留纯圆角** —— 去掉"带气泡 / 指针方向"两档（连同右键菜单里那几项、以及内容里的
    `shape` / `tail` 两个键）；
  - **去掉卡面里的那层底色**：颜色只画在**卡片外壳**上（与图片卡同一手法），卡面透明
    —— 从前"卡壳一层 + 卡面里再一层同色"，看着就是多了一层；
  - **文字默认白色**（快捷操作栏里可改字色）；
  - **加便签卡同款的快捷操作栏**：标记 / 粗 / 斜 / 下划线 / 字色 / 底色 —— **不含「编辑内容」**
    （这行字双击就能改）。于是 `card.icon` 与 `card.titleStyle` 在这张卡上**真的生效**了
    （从前存得进去、却没人画）。

- **图集卡**（`A4`）：**一次拖进两张以上图片**就合成**一张**图集卡（一张图仍是图片卡）。
  卡面：当前那张铺满，**后面两张露一点边**（错开 + 缩小 + 降透明度）—— 一眼看出"不止一张"；
  右下角 `3 / 7`；两侧**悬停出现**上一张 / 下一张箭头，**双击图片翻到下一张**（单击留给选中 / 拖动）。
  翻页到头**回环**；`index` 越界会夹住；这一组里某张图丢了会**点名**说明是哪一张；
  导出 Markdown 是一组 `![[…]]` 内嵌，断链总览里每张图各算一条引用。

- **视频卡 / 音频卡的配色与边框**（用户 2026-09-18）：两张卡的**底色与边框都跟卡片颜色走**
  （与图片卡同一套机制）—— 新建的视频卡默认**纯黑**、音频卡默认 **`#261f1b`**（暖近黑，像木壳 + 黑胶）；
  在调色板里改个色，画面外那一圈一起变。右键菜单新增「**取消边框**」（边框 / 底色 / 阴影一起收掉，
  视频卡连内容槽那圈内边距也归零）；**标题默认不显示**（与图片 / 文件卡同一条规则）。
- **音频卡**：默认尺寸改成 **9:10**（198×220，唱片是圆的，略高于宽才给下面那行控制条留出地方）；
  **胶碟部分固定纯黑**（不跟主题 —— 唱片就是黑的），纹路改用半透明白，黑底上才拉得出层次。

- **仅标题卡**（`A3`）：整张卡就是**一个圆角块**（只有一行字、没有内容区、**没有边框**），
  文字按卡片颜色自动反色（深底白字 / 浅底黑字）。两档样式：**纯圆角**（缺省）与
  **带气泡**（三角指针，方向可切上/下/左/右，切回纯圆角时方向自动清掉）；
  双击进**单行就地编辑**（`Enter` 提交 / `Esc` 放弃 / 没改不写盘）。
  样式切换在卡片右键菜单里。入口：左工具栏「更多卡片 → 新建标题卡」。

- **音频卡**（`A2`，**留声机**）：拖一个 `.mp3` / `.wav` / `.m4a` / `.flac` / `.ogg` 等进来，
  落下的是一张**正方形唱片卡** —— 卡面是 CSS 画的唱片（同心圆纹路 + 中央标签），
  **播放时转起来**（暂停停在当前角度，接着播接着转）；底下一行控制：
  播放 / 暂停、**可点的进度条**（点哪跳哪）、`0:25 / 1:40` 时间、**音量滑杆 + 静音**。
  双击仍是用系统应用打开；导出成 Markdown 走 `![[…]]` 内嵌。

- **视频卡**（`A1`）：拖一个 `.mp4` / `.webm` / `.ogv` / `.mov` 进来，落下的就是**一张播放器**
  （不再只是"文件卡的视频形态"）：卡面就是画面、**16:9** 默认尺寸，真实高度按文件的固有比例自动重算
  （竖屏视频也不变形）—— 这就是"等比缩放"。播放键 / 封面（`preload="metadata"` 白捡首帧）
  与文件卡**共用同一套实现**；双击仍是用系统应用打开；导出成 Markdown 走 `![[…]]` 内嵌。
  入口：左工具栏「更多卡片 → 新建视频卡」。老的 `.nboard` 里那些"文件卡形态的视频"照旧能播、一个字节不改。

### 批 1 · 手感与样式收口（已落地）

- **图片卡清晰度**：判据乘上 `devicePixelRatio`（Retina 上 320pt 的卡片要画 640 个**设备**像素，
  从前按 CSS 像素算 ⇒ 256 的缩略图被撑开、看着糊）；同时修掉"后台生成好的缩略图把**已经决定
  用原图**的卡片换回去"。新增设置项「**图片卡始终使用原图**」（**默认开**；关掉回到自动判定、省显存）。
- **色板卡**：默认尺寸改成 **3:4 竖版（180×240）**；悬停不再铺一层灰底（那会盖住"这一格到底是什么色"），
  改成给色号加一条与它同色的虚线。
- **文件卡**：新版式 —— 左侧 44px 的**扩展名方块**（按类型着色，底色由同一个颜色调出来）+
  右侧两行（文件名 /「**大小 · 修改时间**」）+ 右上角悬停浮出的「**打开**」按钮。
- **链接卡：迷你书签样式**（右键 →「紧凑书签样式」）：只画一行"站点图标 + 域名"，
  卡片同时换成 220×40；一键换回（内容与尺寸落在**同一步**撤销里）。
- **左工具栏**：任务卡 / 色板卡 / 地图卡收进「**更多卡片**」二级菜单（点开是 Obsidian 原生菜单）；
  主条只留"建之前要先问一句"的那几张（便签 / 评论 / 图片 / 文件 / 链接）。
- **右键菜单整理**：**空白处** ——「新建便签」留在最上面一格，其余"空白纸"（同步便签 / 评论 / 任务 /
  色板 / 链接 / 分栏）收进「**更多卡片**」子菜单（与左工具栏同一套形状）；
  **卡片上** ——「对齐 / 等距分布 / 编组 / 分栏」收进「**排列与编组**」子菜单（一级项从约 20 行降到 14 行）。
- **标记（emoji）选择器改成网格**：按 10 个分类分组、八列网格；输入框两种用法 ——
  打 / 粘一个 emoji 就在最前面钉一格"就是它"（系统表情面板挑的也选得中），打文字则按分类名筛；
  当前那个用一圈选中环（不是往格子里加 `✓`，那会把格子撑变形）。
- **文件列表右键**：「在此新建 nestboard」＋新增「**在此新建 nestmind**」（建在右键的那个文件夹里）。
- **脑图 · 四级及更深子节点不再画盒子**，只留一条贴住文字的**托底线**（三层及更浅照旧带色）；
  字号与几何一个像素不改（导出同步）。
- **左工具栏排序**（用户 2026-09-18）：「新建白板卡」挪到「新建便签」之前；便签之后新增「**新建标题卡**」。
- **白板卡迷你形态**（用户 2026-09-18）：圆角 **30**、默认主色改为 `#6b84ff`、编辑标题时输入框字色取黑
  （显示态仍是卡片标题色）。
- **新建 / 拖入的子白板入口**：落卡时补上**默认内容**（`preview: 'mini'` + 随机记号 + 计数开关）与
  **按类型取的默认色** —— 这两样此前都在落卡这条路上被丢掉，于是新建的子白板看着像没建成功。
- **标题卡**（用户 2026-09-18）：字号调大；接入便签同款的快捷操作栏
  （标记 / 粗 / 斜 / 下划线 / **字色** / 底色）；那行字改读**卡片标题** `CardBase.title`（不再是内容）。
- **分栏可设色**（用户 2026-09-18）：右键「颜色」子菜单（6 档主题色 + 自定义取色），标题栏背景跟主色走、
  字色按底色对比度自动取黑 / 白；**新建分栏默认白**。
- **音频卡默认色**改为 `#FE232D`。
- **色板卡色号**在 `:focus` / `:focus-visible` / `:active` 下也不再铺底（此前只有基态与悬停是透明的）。

### 修复

- **链接卡抓到的站点信息不再丢**：从文件读回时 `siteName` / `icon` / `finalUrl` 三样从前**一个字都不读**
  ⇒ 重开白板之后"获取预览"抓到的信息全没了、卡面又变回裸域名。现在原样读回（空值键依旧缺席）。
- 全量跑时**偶发**红的那条用例（系统文件串行导入）改成"等事情真的发生"，不再拍一个固定时长。

- **标题卡改字色要等下一次重绘**（用户 2026-09-18）：内容指纹只序列化了 `content`，而仅标题卡把
  `titleStyle` / `icon` 直接画在内容槽里 ⇒ 指纹没变、`renderContent` 提前返回、卡面不重绘 ——
  表现就是改完字色要双击点开那一下才生效。现在三样一起进指纹（`CardLayer.stringifyContent`）。
- **音频卡选了红、重开又变回默认色**：归位逻辑从前认等于全局默认色就回退，用户主动挑的颜色会被误判成
  没挑过；现在只迁移老版本那个具体的近黑色 `#261f1b`，用户挑过的一律保留。
- **旧版标题卡的文字**存在 `content.text` 里，打开时自动搬到卡片标题（迁完清空 `text`），老卡片不丢字。
- **标题卡那行字显示的是内容、不是标题**：它没有标题 + 正文两段，这行字现以 `CardBase.title` 为准，
  编辑标题与属性面板的标题栏读的都是同一处。

### 变更

- 色板卡**新建时**的默认尺寸：240×160 → 180×240（**老卡片不受影响** —— 尺寸是数据，不是样式）。

---

## [未发布]（v2.0.1 候选）

### 新增

- **脑图视图（`.nestmind`）**：与白板**平级的第二个文档类型**——同一个插件、同一套读写与撤销，
  点文件 / 内部链接 / 文件浏览器都直接进脑图。
  - **编辑**：`Tab` 加子节点、`Enter` 加兄弟、`Shift+Tab` 提升、`F2` 改标题、`⌘⏎` 改内容（节点下一块 Markdown）、
    `Space` 折叠、`Delete` 删子树（**根删不掉**）、方向键在树里走、`⌘A` 全选可见节点、`⌘C/X/V` 子树复制粘贴。
    **不碰鼠标能从空图敲出一棵三层的树**；每一步都 `⌘Z` / `⌘⇧Z` 可退。
  - **鼠标**：拖节点改父（拖到节点上 = 成为子节点、拖到空白 = 变悬浮节点）；拖拽时一条虚线环圈住"会挂到谁下面"
    （落在自己的子孙上时**变红**，明确拒绝）；拖到折叠的节点上停一下会替你展开；`D` 开关辅助线；空白处左键框选。
  - **布局与样式**：**根 30px 加粗 / 一层主题色 18px / 二层淡粉 14px / 三层及以下纯白 14px**，
    一眼看出第几层；新建的脑图把中心主题摆在视口正中；折叠手柄长在连接处（收起时显示整支节点数）。
  - **画布调节（左下角）**：缩放（`−` / 百分比 / `+` / 适应内容，与 `⌘=` `⌘-` `⌘0` 等价）+
    **总体结构**（逻辑图向右 / 向左 / 八爪鱼 / **组织结构图向下**）+ **分支线**（曲线 / 直线 /
    直角折线 / 圆角折线）。★ 结构与线型那两处**用图标**（点开是"图标 + 名字"的菜单），
    悬停显示全名。它们跟着文件走、**不进撤销栈**（与缩放同一条纪律）；
    四档结构（向右 / 向左 / 八爪鱼 / 组织结构图向下）**全部可用**（鱼骨图已砍掉，见下）。
  - **节点右键菜单**：剪切 / 拷贝 / **原地复制**（紧挨着原节点再出一支）/ 粘贴为子节点 / 编辑内容 /
    删除 / 折叠·展开 / 打开附件 / 删除附件 / 加子节点 / 加兄弟节点 —— 做不了的项**置灰**、不做的不出现。
  - **快捷操作栏（画布下方居中）**：选中单节点时出现 —— **标记**（emoji，分组挑选、标题最前、一个节点一个）、
    **加粗 / 斜体 / 下划线**（作用于**整条标题**，`⌘B` `⌘I` `⌘U` 同效）、**标题字色**与**标题底色**
    （各几个小色块 + 「默认」）、**编辑内容**、**插入图片**（走与拖入同一套落库）。
  - **附件（`B`）**：从文件浏览器 / 系统拖文件到节点上。**一个节点一个附件**（拖新的即替换）；
    非图片在标题带末尾显示一个**回形针**（悬停显示文件名、**点击打开**，文件不在了会明说）；
    **图片**直接铺在标题上面（**不显示回形针**，双击图片打开），四个角可以**等比**拖动改尺寸，尺寸存进文件、`⌘Z` 可退。
    节点右键菜单：打开附件 / 删除附件。
  - **导出**：命令面板「脑图：导出为 **Markdown** / **SVG** / **PNG**」——Markdown 按树倒成标题层级 + 正文 + 附件链接；
    SVG / PNG 用**屏幕上同一份布局几何**重画（任意倍率都清晰），落在脑图旁边、名字顺延不覆盖。
  - **反链**：脑图节点正文里写的 `[[链接]]` / `#标签` 进**跨文档反链面板**（"谁提到了这篇笔记"现在也认得 `.nestmind`），
    点一条会打开那份脑图并**定位到那个节点**。
  - **框选多个节点时，可以统一改样式**：选中 ≥ 2 个节点，底部的快捷栏换成**缩减版**
    （加粗 / 斜体 / 下划线 / 标题字色 / 标题底色）—— 「标记」「编辑内容」「插入图片」
    对一堆节点没有唯一含义，所以**不出现**（不是置灰）。值取"**全一致才亮**"：
    混合态那一格不亮，点一下是"**全都设成一样**"（而不是"把已有的翻掉"）。
    一次改动**一步 `⌘Z`**（改 5 个节点也只记一步）。
  - **结构与线型的图标整套换新**（用户 2026-09-16 给的统一一套）：解析规则跟着改对了三条 ——
    `<defs>` / `<clipPath>` 里的模板一个都不收（此前那个 26×26 的裁剪矩形会被当成图形，
    在图标上盖出一个**黑框 / 主题色框**）、`<g transform>` 下发到子形状（否则形状按组内坐标
    画出来、**乱成一团**）、"只描边"与"实心字形"两套素材分别走两条路（`fill` 该是 `none`
    还是 `currentColor` 由形状自己说了算）。
  - **关联线默认是虚线**（一眼与"父子关系"的实线分开），每条线可以自己改成**实线**：
    右键线身 ⇒ 虚线 / 实线两档（当前那档打勾）。导出 PNG / SVG 跟着同一条线型。
    另外顺手解掉一处不一致：拖动时的**预览**本来就是虚线，从前落笔却变成实线 ——
    现在两边长得一样了。
  - **大纲里能复制文字、也能框选多行**：
    - **复制文字**：用鼠标划过一段文字按 `⌘C` / `⌘X` ⇒ 走系统复制（不再被"复制整行"抢走）；
    - **通用黏贴**：从别处复制一段字，选中一个节点按 `⌘V` ⇒ **一行变成一个子节点**；
    - **框选多行**：按住往**下**拖（竖着 / 斜着）即可框住扫过的那些行 —— **横着**拖则是选字；
      框住的会一起选中（**左侧一条强调色竖条 + 很轻的中性底**标出选中的行），
      接着 `⌘C`、再选某个节点 `⌘V`，就是把这几个节点贴成它的子节点
      （`⇧` 加选、`Esc` 取消；从行的左侧空白 / 竖线格 / 面板空白起手也直接是框选）；
    - **粘节点还是粘文字，看格式不看场合**：复制节点时会往系统剪贴板同时写
      "**可读的缩进文字**"（粘到笔记 / 别处就是一段像样的文字）和"我们自己的格式"；
      于是在**标题编辑框里按 `⌘V`** 也能分辨 —— 剪贴板里是我们复制的节点就**贴成这一行的子节点**，
      是普通文字（从网页 / 笔记里复制的）就照旧粘成文字。
    - `⇧`+点击一行 = **只选中 / 范围多选，不进编辑**（点同一行 = 只选中这一行）——
      这样"想多选但不想改字"不必再靠框选。
    - 顺带修好：**点一行的文字 = 光标落在你点的那个字之间**（框选不再抢走这一下）、
      **点空白处 = 取消多选**、**进节点后的面包屑能点着回上一层**（同一处指针捕获的毛病）、
      编辑时**光标用强调色**更好认。
  - **关联线能改颜色了**：右键一条线 ⇒ 菜单里有「默认颜色」+ 六个**主题色**（当前那项打勾）；
    颜色存的是主题色编号 ⇒ **换主题时线也跟着变**；箭头与线同一个颜色，导出的图里也是。
  - **关联线能调弯折了**：选中一条线 ⇒ 它的中点出现一个**手柄**，拖它即可调节这条线的
    **弯折程度与方向**（拖到哪儿、线就弯到哪儿；一次拖动 = 一步撤销，`Esc` 可放弃）。
    右键菜单多一项「**把线拉直**」。★ 没调过的线（以及存量文件）画法与从前**一个像素都不变**。
  - **脑图的导出也进了 Obsidian 自带的「…」菜单**（与白板同一个入口），并新增两种导出：
    - **导出为 XMind（`.xmind`）**：符合 XMind 规范（`content.json` + `metadata.json` + `manifest.json`），
      保留标题 / 层级 / 内容与附件路径（写进备注）；标记、撞色、完成态按规范裁掉；
    - **导出为大纲式 Markdown**：根节点是标题（`#`），一级 `##`、二级 `###`，
      **三级及更深是缩进的正文**；节点上的内容一律变成代码块。
  - **分栏里的卡片不再"歪着排队"**：拖进分栏时角度当场归 0，之后栏内也不会再出现旋转手柄
    （历史数据里已经歪掉的，用右键菜单的「重置角度」掰回来）；
  - **图片卡加了阴影**；另外修好了上一版把「左右分屏 / 上下分屏」挤没的问题。
  - **视图菜单改为注入 Obsidian 自己的「…」**：白板右上角那枚自绘的「⋯」浮标去掉了，
    「导出 PNG / SVG / PDF / Markdown、复制白板链接、打开 Home、适应全部内容」现在长在
    标签页右上角 Obsidian 自己的「…」菜单里（就在「左右分屏 / 上下分屏」下面）。
  - **圆角折线只圆孩子那一端**：从主干（父节点那侧）分出来仍旧是方角，只有快到子节点时
    才倒圆角 —— 看起来更像"从主干上分出来"。
  - **图片卡一轮（五项）**：
    - **大图不再糊**：用缩略图还是原图，改成按"这张卡在屏幕上要画多少像素"判断
      （从前只看缩放倍数，800px 宽的截图卡会被 256px 的缩略图硬撑）；
    - **黑色相框**：新建的图片卡默认纯黑底 + 纯黑边框，圆角更大；
      **改卡片颜色时边框跟着变**；
    - **说明文字**挪到卡片**下方外侧**，平时不显示、鼠标移上去才出现（双击仍可就地改）；
    - 右键菜单新增「**取消边框** / 显示边框」——取消后底色、边框、阴影一起收掉，只剩照片；
    - **存量的图片卡也会变成黑相框**（只改"你从没挑过颜色"的那些 —— 自己挑过的照旧）。
  - **撞色标题带上的文字默认白色**（与彩色底形成反差）：便签卡的标题带、
    脑图的**根节点与第一层子节点**；二层及以下仍是按浅底挑的深字，
    自己从快捷操作栏挑过字色的照旧听你的。
  - **修两个白板上的 bug**：
    - 编辑态按 `Esc` 时画布**不再被描一圈框**（那是一圈键盘焦点轮廓，不是框选）；
    - 双击白板卡**点开子板**时，子板里**不再凭空多出一张便签**（那一下双击会"漏"进新板）。
  - **缩略图导航器（右下角小地图）**：树视图里一眼看清"这张图多大、我在哪一块"，
    点击 / 拖动即可跳转，`Tab` + `Enter` 回到内容中心。默认关闭 —— 设置 →「缩略图导航器」
    打开（或命令面板 →「切换缩略图导航器」），**一个开关同时管白板与脑图**；进大纲视图自动退场。
  - **附件断链提示**：附件被删 / 移出库 ⇒ 节点上那枚回形针**变灰**，悬停说明"附件不在了"
    （点它也会说一句）；**改名 / 库内移动会自动跟随**，不用手动重挂。
  - **树视图里改标题时按 `⏎` = 提交并接着新建一个同级**（幕布导图的手感）：
    光标直接落在新节点里，可以一路 `⏎` 写下去；`⌘⏎` / `Esc` 仍是"改完就走 / 放弃"。
    顺带修好"提交之后键盘失灵"（输入框被移除时焦点掉回 `<body>`，画布再也收不到按键 ——
    从前必须用鼠标点一下画布才能继续按 `⏎`）。
  - **大纲里像改文本一样改结构**（`⏎` 拆行、`Tab` 缩进）：
    - 光标在文字**中间**按 **`⏎`** ⇒ 前半截变成**上面一个新节点**，原节点留后半截
      （**它原来的子节点仍跟着它**），光标停在原节点最前面、可以直接接着打字；
    - **新建一个子节点后按 `⏎` 即可继续建同级**（光标就在新节点里，不必先点它一下）；
      给**中心主题**新建的空子节点按 `⏎` 也不会再"没反应"（升不动时会退化成新建同级）；
    - 光标在**行尾**按 `⏎` 按幕布那三条来：**收起 / 叶子** ⇒ 下方新建同级；
      **展开着** ⇒ 建一个**第一个子节点**（看着像"新建了一行，但是缩进的"）；
      **空行且排在最末** ⇒ **提升一级**（取消缩进）；
    - **`Tab`** ⇒ 这一行**缩进**成上一行的最后一个子节点（自己那一支一起进去），
      **`⇧Tab`** 缩回去。★ 光标在不在字里，`Tab` 都是这个意思；
      "新建**子**节点"因此挪到 **`⌥⏎`**（原先挂在 `Tab` 上）。
  - **导出 FreeMind（`.mm`）**：命令面板 →「脑图：导出为 FreeMind（.mm）」⇒ 脑图旁边多出一个
    `.mm`。它是**朴素 XML**，FreeMind / Freeplane / 幕布都能打开，而且**还能接着编辑**：
    树、**折叠状态**、**主色**、**关联线箭头**都带过去（左右两支的分布与画布一致）。
    ★ 附件不导出（那边 `LINK` 是 URL 语义）、加粗斜体不导出（要么把节点文字整段改成富文本，
    不划算）—— 这两条取舍写在 `06 §11.51` 的映射表里。
  - **完成 / 激活**：大纲里按 **`⌘⇧⏎`**（或右键那一行 →「标记为完成」；画布上右键节点也有）
    ⇒ 标题加**删除线**、它**下面整支变淡**；再按一次即取消。★ 只是"这一条做完了"这一个标记，
    **子孙节点自己的状态一位都不动** —— 所以取消时不会把子孙里本来完成过的那些弄丢，
    一步 `⌘Z` 可退。导出 PNG / SVG 里也画（整块变淡 + 标题一条删除线）。
  - **文字高亮**：选中节点 ⇒ 快捷栏点 `🖍` ⇒ 挑一支浅色（黄 / 绿 / 蓝 / 粉 / 橙 / 紫）
    ⇒ **文字后面出现一块底色**（像荧光笔划过去），大纲里同样生效，**导出 PNG / SVG 里也有**；
    与「字色」「标题底色」是三件不同的事。再点那种「无高亮」即可恢复。
  - **大纲行首圆点的手势（最终版）**：**单击圆点 = 进入这一层**（与 `⌘]` 同一个动作）；
    **右键这一行 = 这一行的菜单**（单击让给了"进入"，菜单挪到右键）；
    **按住圆点拖** = 拖拽调整结构；**折叠 / 展开**仍然归左边那个三角。
    ★ 一个坑记在这里：按在圆点上会启动拖拽（指针捕获在面板上），而**指针被捕获后浏览器会把
    `click` 派发给捕获元素** ⇒ 挂在圆点上的 `click` / `dblclick` 监听器根本收不到
    （症状就是"单击没菜单、双击没效果，但拖拽是好的"）。所以圆点上的手势**全部走指针状态机**。
  - **大纲里可以「进入当前主题」了**：选中一行 ⇒ **`⌘]`**（或点行首圆点 →「进入当前主题」）
    ⇒ 列表只剩这一支、把它当作"根"，顶上出现一条**可点的层级导航**（`整棵树 › 中心 › 甲`）；
    **`⌘[`** 返回上一级（到第一层再按就退出聚焦），点导航任意一格也能跳过去。
    聚焦状态存在文件里 ⇒ 关掉重开还停在这一支；**进出聚焦不进撤销栈**（`⌘Z` 退的仍是你做的改动）。
  - **大纲里可以拖了**：按住某一行的**行首圆点**上下拖 ⇒ 那一行（带整支）跟着走，
    落到另一行的**上 / 下沿** = 插成同级的前 / 后，落到**中间** = 变成它的子节点
    （拖到自己的子孙上是**红框**，落不下去）；松手即生效、一步 `⌘Z` 可退；
    **只按不动**仍然是"点一下圆点开这一行的菜单"。
  - **大纲三处返工**：行首那个小黑点**去掉了外面那层圆底**（叶子 / 完全展开时就是一个点）——
    那层底的真正来源是"圆点 / 三角是 `<button>`，被 Obsidian 自带的
    `button:not(.clickable-icon)` 底色 + 阴影环盖住"，改成 `<span role="button">` 才干净
    （插件自己的类压不住主题的元素级规则）；折叠三角**平时完全不显示**、鼠标移到那一行才出现（比原来大一档、也没有外圈）；
    **点一行进编辑时文字不再往下跳**（输入框改成"盖在原来的位置上、不参与布局"），
    并且**不再自动全选**（不会一进去就是一片淡紫的选区）。
  - **关联线可以选中、改标签、加箭头、删掉了**：点线身 ⇒ 选中（换成强调色，节点选区随之清空）；
    **双击线身**（或选中后按 `⏎`）⇒ 在线中点**就地写标签**（清空即去掉标签）；**右键线身** ⇒
    菜单里有 **编辑标签 / 无 / 单向 / 双向 / 删除关联线**（当前箭头那档打勾）；选中后按 `Delete`
    删线（一步 `⌘Z` 可退）。**导出 PNG / SVG 里也画**（线、箭头、标签都在，与画布上同一份几何）。
  - **关联线可以画了**：选中一个节点 ⇒ 快捷操作栏最右的 **`↗` 连线** ⇒ 从它拉出一条**虚线**
    跟着鼠标走 ⇒ 移到别的节点上时虚线**吸附**过去（那个节点罩上虚线环）⇒ **左键**落下成线
    （一步 `⌘Z` 可退）；`Esc` / 右键中途放弃，不留任何痕迹（连到空白处不给落线 —— 关联线必须
    连着两个节点）。**连线的起点必须是唯一的"这一个"** ⇒ 多选时那一格不出现。
  - **关联线（第一步：数据与画出来）**：脑图里可以有两节点之间的**额外关联**（`links`）——
    与"树的父子分支线"是两回事：**不参与布局**（画一条线不会让任何节点挪位置），
    **只有曲线一种样式**，端点永远贴在两个节点**面对面**的那两条边上。
    节点一移动 / 折叠，线跟着端点走；删掉端点节点，线自动消失。
    （"从节点拖出一条虚线、吸附到目标、左键落下"的手势与标签 / 箭头 / 删除是接下来两步。）
  - **修**：导出 PNG / SVG 里，除根节点外每个节点**底下露出一截直角**
    （"像贴了一小块方纸"）—— 标题带的高度算了两遍（画带子那个函数自带一个 `min(height, 44)`），
    矮节点上带子会铺满整张卡却只圆了上面两个角。现在高度只算一处，
    铺满整张卡时**四个角都圆**。
  - **修**：改关联线标签时，输入框**不跟着打字变长**（宽度一直停在打开时那一刻）。
  - **大纲视图按「思维笔记」那份产品规格又对齐了一轮**：
    **整行 hover** 现在有一层很轻的底（层级一深、行一密，"我在操作哪一行"才看得出来），
    选中态比 hover 更明确一点（键盘的 `Enter` / `Tab` / `⌫` 都作用在它身上）；
    **行首小圆点做成了"节点把手"** —— 点它弹出这一行的菜单（编辑描述 / 删除 / 折叠展开），
    并有三档状态（默认很浅 → 移到这一行变明显 → 压在圆点上最深且略放大）；
    **折叠箭头**改成"有子节点就出现、但平时很淡，鼠标移到这一行才清晰"
    （同时满足官方"有子节点就点箭头"与你实测的"悬停才现"）；
    **删除分两种语义**：`⌘⇧⌫` = 连整支一起删，光秃秃的 `⌫` = 只在这一行**是空的时候**删、
    并把**子节点提到它原来的位置**（非空行按 `⌫` 不删 ⇒ 不会手抖抹掉一整支）。
  - **大纲视图：编辑起来像纯文本了**（用户实测反馈）：点某一行文字中间 ⇒ **只多出一根光标**，
    而且**落在你点的那个字上**（不再弹出一个带边框的输入框）—— 字号 / 字重 / 颜色全按那一行照搬，
    开编辑前后这一行看上去一个像素都不变。另外两处视觉修正：**收起时的淡圈**改成一层柔和的浅底
    （原来那圈 1px 描边太丑）；**折叠箭头**不再"点过之后一直亮着一圈底"（那是主题给按钮的焦点样式，
    现在只在你真的把鼠标停在箭头上时才亮）。
  - **大纲视图按飞书「思维笔记」的样式又修了一版**（用户逐条给的）：
    **根节点变成顶上那行标题**（26px / 500 字重 / 一条下边线，**不能直接改** —— 要改回导图改），
    行只从**第一层子节点**开始；每行行首**一个小圆点**，这一支**收起**时圆点外面加一层**淡圆圈**；
    **平时不显示**折叠手柄 —— 鼠标移到那一行才在**圆点左侧**现出三角（三角自己悬停还有一层淡圈底），
    收起时行尾那个数字**点一下就展开**；**行没有背景也没有边框**，
    **点文字任意位置直接进编辑**（不必双击、不必先选中）；`↑↓` 也改成"把光标搬到上一 / 下一行"。
  - **画布空白处右键多了一条菜单**（参考飞书思维笔记）：**折叠所有节点**（折完**只留中心主题与第一层**
    —— 飞书就是这么定的）、**展开所有节点**、**定位到中心节点**（把中心主题摆回屏幕中央）。
    折叠 / 展开都是一步 `⌘Z`；只读时"看"的那项照旧可用。
  - **大纲的样式照幕布重排了一遍**（对着幕布导出的 HTML 里那些原值）：
    每级缩进 **28px** 并画出**一条 1px 竖线**当连接线；正文 **16px / 行高 24**（字号只有三档：
    根 22 / 一级 18 / 更深深 16，加粗用 500 —— 不再是"每层都顶着 700 黑体"）；
    行首一个 **6px 圆点**，有子级时悬停变一圈环、**折叠时填成实心圆**（于是不必再画三角）；
    正文预览 **14px / 行高 22 的灰字**；整页 **max-width 790px 居中**。
    两处刻意与幕布不同：正文预览**不折行**（一行更好扫结构）、不做"每行自己的标题级别"。
  - **大纲视图的收尾**：切走时**记住滚到哪**（按文件记，不落盘），切回来还停在同一处；
    切回导图时镜头**挪到刚才在改的那个节点**上（选区本来就是共用的，不挪的话"我改的东西呢？"）；
    叶子行行首现在也有一个**小圆点**（对着幕布导出的 `bullet-dot` 那一格）。
    切视图与视口同一档：**不进撤销栈、不递增 revision**（`⌘Z` 不会退回"切换"本身），
    两视图**共用一条撤销链**。
  - **大纲视图可以编辑了**：键位按**幕布**的口径对齐（`Enter` 新建同级 / `Tab` 子级 /
    `⇧Tab` 提升一级 / **`⇧Enter` 写正文**（"描述区"）/ **`⌘⇧↑↓` 同级换序** /
    `Space`·`⌥.` 折叠 / `⇧↑↓` 多选 / **直接打字**就开始改那一行）；
    双击一行 = 就地改文本，改完 `Enter` 提交、`Esc` 放弃；正文在行下方一个纯文本域里写
    （写进的是同一个节点正文，回导图就能看到排版）。撤销 / 复制粘贴与导图**共用一条**。
  - **大纲视图（第一步：能看、能折）**：脑图右上角一个「大纲视图 / 树视图」按钮（命令面板里
    也有一条，不给默认键），切过去整棵树变成**缩进列表** —— 层级越深缩进越多、字号跟着层级走，
    标记 emoji 在行首、粗 / 斜 / 下划线 / 字色照常生效，正文在标题下一行**灰字预览**（超出省略号）。
    **悬浮节点不出现**（大纲只编辑主树）；**折叠一支** ⇒ 行尾显示这一支共几个节点（与导图手柄同一个数），
    折叠状态写在文件里、重开还在。切换本身只是"换个角度看同一棵树"：**不进撤销栈**。
    （大纲里的键位编辑是下一步。）
  - **已知边界**：跨白板搜索（`F8-08`）目前仍只覆盖 `.nboard`（它吃的是一整份白板模型）；
    **鱼骨图不做**（2026-09-16 砍掉：形状能画出来，但体验一般、脑图里也不常用 ⇒ 菜单里没有这一项，
    文件里手写的 `fishbone` 会被丢掉并回落到"逻辑图（向右）"）；
    移动端不做；导出里的图片附件画成占位块（不内嵌 base64）；
    没做**属性面板**（节点 `props` 的数据格式已经就位，界面排在后续）。
- **白板视图**：新建 / 打开 `.nboard`（纯 JSON 文件），视口平移、缩放、适应内容、视口记忆，四种画布背景。
- **卡片（12 种）**：便签、同步便签、引用、图片、地图、文件、链接、待办、色板、白板（嵌套）、手绘、评论。
  - **便签卡换上撞色标题栏（`O38`）**：标题栏是这张卡的主色、正文是**白纸**，再配两层阴影 ——
    与脑图节点同一套语言，一眼分得清"标题（观点）/ 正文（细节）"。标题带上的字色按底色对比度
    自动挑深 / 浅，**读得清**这件事不必用户自己调。
    ★ 便签**不再显示**标题栏那个"收起 / 展开"小按钮 —— 收起 / 展开走**右键菜单**（那一项本来就有）。
    ★ 顺带修掉两处旧账：标题的样式变量名原先误用了脑图的（`--nestboard-mind-title-*`，白板上没人写过它们，
    于是标题一直吃兜底值），改回卡片自己的变量、**取值一个都没动**；
    "深色便签在不认 `:has()` 的宿主上会变成白纸"这条隐患也一并收掉（整段套进 `@supports selector(:has(*))`）。
  - 便签卡新增两个**可选键**（为接下来的"快捷操作栏"铺路）：`icon`（卡面标记，一个 emoji）与
    `titleStyle`（标题**整条**加粗 / 斜体 / 下划线 / 字色）；旧文件一个字节都不动。
  - 引用卡双向同步源笔记；源文件改名 / 删除时自动跟随或进入断链态。
  - 图片卡支持说明文字、非破坏性裁剪、缩略图优先渲染。
  - 地图卡贴一张**本地静态地图图** + 一个图钉（双击图上落钉）；换图保留图钉，图与图钉一起进 PNG / PDF 导出。
  - 地图卡还能**粘一条地图分享链接**（`O08`）：右键 →「粘贴地图链接」，认得出坐标就存进卡片（Google 地图 / Apple 地图 / 高德 / OpenStreetMap 的分享链接，以及 `39.9042,116.4074` 这样的纯坐标）。认不出来（短链、手改过的地址）会弹输入框并把**原文预填**进去让你改；剪贴板读不到也能手输。设置了**静态图服务**（默认「不出图」）就顺带取一张图存进附件目录 —— **取图失败不影响已经存下的坐标**：卡上仍有地点名、坐标、链接和一个「打开」按钮，双击卡片也能在系统浏览器里打开它。右键 →「打开链接」随时可开（没贴过时是灰的）。
  - 同步便签卡把**同一张便签摆在多处**：编辑任意一处，整组立刻一起更新（各存一份正文、只留一条写入口，一次提交、一步撤销）；右键「新建同步副本」再摆一处、「取消同步」把这一张脱离出去；在同步组里的那张带「同步」角标。
  - 评论卡是一条**本地备注线程**：逐条追加、每条带写下时的时间戳（本地 `MM-DD HH:mm` 显示，悬停看完整年份）。**只有"追加一条 / 删掉一条"两种写入**——刻意不提供整篇改写，免得"时间线上的一句话"被事后改成另一句。空文本不落条目；坏时间戳记 `0` 但**条目不丢**（显示「时间未知」）。右键「标记为已解决 / 重新打开」只改一个布尔、不删内容（卡面置灰 + 角标）。显示态只留一行「写一条备注」，双击卡片直接进编辑态并聚焦新备注；`Enter` 提交、`Shift+Enter` 换行、`Esc` 放弃草稿、输入法组词中一律放行。
  - 白板卡的卡面**只保留「迷你」一档**（`O18`，用户 2026-09-16："所有白板，只保留迷你形式。其他形式都不需要放出来"）：
    右键里不再有档位选择，**存量文件一打开就归成迷你**（读入口归一 + 尺寸一并钉成正方形、保存即定型）；
    另外三档的渲染代码留着（读得懂手改过的旧文件），只是不再给入口。**迷你**（`O18`）是一个**固定正方形**的方格子（边长 = 便签默认宽的 1/3，约 87px）：卡面上**不预览内容**，正中只放这张卡的 emoji 图标（没设图标就是主题色小方块），**板名画在方格子正下方、居中** —— 名字行与「N 张卡片」**都不存在**。尺寸由这一档定死，选中时**没有缩放的八个手柄**（**旋转**不受影响）；切到这一档当场钉成正方形、切走时还回默认尺寸（`280×180`），这是唯一会改卡片尺寸的档位。一屏摆十几张当目录扫的时候最合用。**实时小窗**把整块子板按卡片**此刻的像素尺寸**重新画一遍 —— 一扇窗，不是一张贴图：卡拖大内容跟着重排重画、整板取景、宽高比不合时由板自己的背景铺满多出来的边，而且**子板一保存这扇窗就更新**。档位写进 `.nboard`（`preview: "thumb" | "mini" | "live" | "none"`），一步 `⌘Z` 可撤销。小窗**只读**（要动手就双击进入那块板）。
  - 笔记里也能嵌一块"活的"板：` ```nestboard ` 代码块里写 `file:` 一行、可选 `height:` 一行 —— 给了 `height:` 就是**只读小窗**（高度按写的来、宽度随笔记栏宽、子板保存即重画），不给就是静态缩略图。行内写法 `![[x.nboard]]` 只取路径、高度随内容。
  - **空栏保留**（用户 2026-09-16，取代原来的「空栏不留空壳」）：把一栏里的卡片全拖出去、
    或者删掉栏里最后一张卡，**分栏都留在原地**（从前会自动消失，用户还得重新建一栏、重新摆位置）。
    现在只有**显式**删除才会让栏消失：栏菜单「删除」、`Delete` 键、「转成编组」（那一栏被新组取代）、
    以及「拆栏」之后被拆空的源头栏。
  - **框选可以框住分栏**（用户 2026-09-16）：框住一栏 ⇒ 选中**那一栏**（一次能框住好几栏，
    也能和"栏外的卡片"同时选中）；**栏内的卡片不再被框选** —— 它们的坐标是栏算出来的、
    栏一收起就看不见，框到手里一拖就会散到别处。要动栏里的卡：点它。
    框住一栏**或好几栏**再 `⌘G` = 把这些栏的**全部卡片**（加上选中的松散卡片）收成**一个**编组
    （与"多张卡片编成一个组"同一条语义），那几栏随之消失，一次提交、一步 `⌘Z`；
    空白处右键也有「编组 / 取消编组」，**分栏右键的「转成编组」也作用于整个选区**。
    `Delete` 一次删掉所有选中的栏，折叠 / 展开对所有选中的栏统一生效。
  - **分栏也能被编进组**（用户 2026-09-16）：`⌘⌥G` 编组时，选区里的分栏是**像卡片一样的成员**
    （`Group.columnIds`）—— 编完之后**栏不消失、栏里的卡片一张都不动**，只是多了一圈把它们
    框在一起的编组框：点组里任意一栏即选中整组、`⌘⇧G` 或右键「取消编组」整组解散。
    与「整栏转编组」正好相反：那个是拿栏**换**组，这个是把栏**编进去**（`⌘⌥G` 对一栏也走这条）。
  - **编组的默认键位改成 `⌘⌥G`**（取消编组仍是 `⌘⇧G`）：原来的 `⌘G` 被 Obsidian 自己占着
    （「打开关系图谱」，而且白板内的「搜索下一个结果」也是它）⇒ 按下去根本到不了白板。
    `⌘G` 这边**照样接受** —— 去「设置 → 快捷键」把关系图谱挪开之后它就归白板。
  - **白板卡的快捷操作栏**（`O38`，与便签共用 `ui/QuickBar`）：选中一张白板卡时底部出现
    **标记 / 卡片颜色 / 编辑标题** 三个按钮。标记与便签走**同一个分组选择器**（右键菜单的
    「卡面图标…」也直接打开它，不再是搜索式弹窗）；「编辑标题」就是**就地改名**（输入框落在卡外那行字上，
    连子板文件一起改）。按钮集按卡片类型给，用不上的**不画**（不是置灰）。
  - **新建白板卡**直接就是**迷你正方形**，并**随机带一个 emoji 记号**（事后可换可清）。
    迷你卡是**纯色块 + 两层阴影、没有描边**；名字画在卡外正下方（编辑时输入框就在那一行，卡内不出现输入框）。
  - **迷你白板卡拖进分栏、再拖出来不再变形**（`O18`）：分栏会改写成员宽高，而"拖出分栏"的约定是
    **几何原地保留** ⇒ 出栏时把这一档的尺寸钉回正方形（删除分栏选"释放卡片"同理），
    且与几何写在**同一个 `mutate`** 里（一步 `⌘Z` 回到原状，不必按两下）。
  - 白板卡的名字左边可以挂一个 **emoji 记号**（`O10`）：右键 →「添加图标」弹出选择器（输入、或从系统面板粘贴任意 emoji，回车选中；当前那个带 ✓），设了图标后这一项叫「更换图标」，「清除图标」把那一格还原成主题色小方块（**只在有图标时才出现**）。记号存的是 **emoji 字符本身**（不是图标名），所以换主题、换版本都还是同一个表情；它写进 `.nboard`、一步 `⌘Z` 可撤销。没设图标时那一格是主题强调色小方块（老样子）。白板卡也能**当连线端点**（悬停出锚点），改色 / 改名走的是所有卡片共用的那两条通路。
  - **色板卡可以放渐变色格**（`O07`）：双击进编辑态，一行一格 —— 一行 `linear-gradient(135deg, #FF6B6B, #4C8DFF)` 就是一格渐变，和纯色格（`#4C8DFF`）混着摆都行；行首带 `- `/`* ` 也认。方向写 `90deg`（负角度会归到 `0°~360°`）或 `to bottom right` 这类关键词，**位置可省**（省了就是均分，卡片不会偷偷补一个 `0%`）。点一下复制的是**那一行文本**，PNG / SVG / Markdown 导出也是它 —— 抄出来的那句原样贴得回编辑框。**认不出来就整行判错**并指出是哪一行，绝不会"先按第一个颜色凑合"（那正是"渐变变成纯色、几天后才发现"的来路）。编辑态始终是源码回显，不拆成表单。
  - **色板卡是"一卡一色"**（`O19`）：**第一格的颜色直接铺满整张卡面** —— 不再是外观配色那层 14% 的淡底，
    吸管取色 / 编辑态贴 hex 都写进同一处、**再次取色是替换**而不是并排多一格。卡上的文字颜色按底色亮度自动挑
    深 / 浅（渐变取第一个色标），任意颜色下都读得清。旧的多色卡打开时显示第一色、其余仍留在文件里（**数据形状
    没变**，`colors` 还是数组），改一次即自然收敛；第一格之外还有颜色时卡角标一个 `+N` 交代。
  - **便签卡可以是深色**（`O06`）：右键 →「深色便签」把**这一张卡**变成黑底白字，再点 →「浅色便签」切回来（菜单标题写的就是点下去会发生什么，图标 `🌙` ⇄ `☀️`）。黑的是**整张卡**（连标题行一起），卡片的**颜色与强调色条不变**（深色只改"底有多暗"，不改"这是哪一种颜色"），正文照旧是完整 Markdown（双链、标签、代码块、列表都在，代码块配深色底）。它写进 `.nboard`、一步 `⌘Z` 可撤销，PNG / SVG 导出跟着变 —— 因为这是**存进文件的内容**，不跟随 Obsidian 主题，别人打开看到的是同一张黑卡。只有便签卡有这一项（同步便签的正文是整组共用的，判据见 `isDarkNoteCard`）；多选 / 锁定时置灰。
  - **便签卡的编辑态是"标题框 + 正文框"两格**（`O22`）：双击便签 / 同步便签进入编辑后，上方是卡片标题的输入框、下方是正文编辑器，**两者可以分别编辑**（与待办卡同一套交互：标题框 `Enter` / `Tab` 把焦点交给正文，点卡片外面才收工）；只想改正文时用 `⌘`+双击或右键 →「编辑内容」（跳过标题那一格）。标题与正文**并成一次写入**（一步 `⌘Z` 就能整条撤掉）。新建便签显示态顶部默认画那行名字（`showTitle` 默认 true），文件里没写该字段的旧便签保持原样。
- **静态图服务设置**（`O08`）：设置 → Nestboard → 隐私与网络里新增「地图卡静态图服务」——
  **不出图**（默认，一个请求都不发）/ OpenStreetMap（不用 key，第三方社区服务）/ Google / 高德，
  后两档要填自己的「静态图服务 API Key」。换档后**再贴一次链接**就会去取图，取回的图存进你的附件目录。
- **链接卡抓得更全**（`O20`）：一张链接卡现在能带**站点名**、**站点图标**、**摘要**与**最终网址**（跟着短链 /
  跳转落到的那一页）。这些字段**全部来自同一次抓取** —— 图标只从已取回的 HTML 里找（`og:logo` →
  `apple-touch-icon` → `icon`），也不用图标聚合服务；图标保持远程地址
  **不落盘**，加载失败时退回本地画的**字母徽章**（这一格是身份标识，必须有东西）。最终网址**只在和原址不同时**
  才记，取不到的空字段一概不进文件。
  > **抓取预览的默认值改成"开"**：新装 / 旧数据（本来没有这个字段）→ 开，显式关过的**保持关闭**（不改用户
  > 已经表达过的意思）。授权点没有变 —— 仍然是**你点那一下**（卡片上的按钮 / 右键菜单）才发一次请求，
  > 渲染、导出、翻板照旧零请求。
- **没有 og 标签的网页也能抓到预览**（`O23`）：此前只认规范 Meta 标签，没有的那些页面（老站 / 博客 / 电商）
  是"一个字段都读不到 → 获取预览失败"。现在在**同一份抓回的 HTML** 里补三条兜底：标题退到首个 `<h1>`（再退
  `<h2>`）、摘要退到首个 `<p>` 的纯文本（截到 100 字）、Logo 退到带 `logo` 关键词的 `<img>`、最后是站点根
  `/favicon.ico`。**有规范 og 标签的页面行为一字不变**；这些兜底**都不新增网络请求**（`/favicon.ico` 只是往卡片
  写一个猜出来的地址，渲染时才可能去取一次，失败退字母徽章）。
  - **抓取请求头带浏览器 `User-Agent`**（`O24`）：不少站对无 UA 的请求直接 403 或回空壳；**只带 UA，不带
    Cookie / Referer**。
  - **明确不做**：AI 语义解析（要联网到大模型，与"一次点击一次请求"和零依赖都不相容）、无头浏览器渲染
    （插件体积与移动端不可行）—— 纯 JS 渲染（SPA）或登录墙后的页面**抓不到就是抓不到**，卡片仍可手动补标题 / 摘要。
- **默认快捷键收窄**（`O25`）：插件只给"撤销 / 重做 / 复制 / 剪切 / 全选 / 搜索 / 缩放"留默认键；
  其余命令（`⌘⇧E` 新建便签、单键 `D`/`E`/`V` 画笔、笔宽 `1`–`4`、`⌘⇧L` 锁定、`⌘U`、`⌘[` / `⌘]`、
  `⌘D` 复制卡……）**一律不再默认占用**，全在 设置 → 快捷键 里自己绑。命令本身与命令面板条目一个没少。
- **工具条收起了「同步便签」**（`O26`）：工具条不再放这一项；命令面板的「新建同步便签」与卡片右键的
  「新建同步副本」都还在。
- **色卡更好用**（`O27` / `O29`）：卡面**那条边**现在跟随卡面颜色（不再出现"蓝底橙边"）；
  **双击色卡直接弹调色板**（与右键「卡片颜色 → 自定义颜色」同一个组件），选中即换色；
  想改色号文本 / 渐变仍走右键「编辑内容」。色号文字的墨色按底色深浅自动黑 / 白（`O19` 起已如此）。
- **卡片可以收起 / 展开**（`O31`）：标题行上有一个小三角，点一下**只留标题那一行**（内容收起来），
  右键菜单里也有「收起卡片 / 展开卡片」。收起状态**写进 `.nboard`**，重开白板仍保持；一步 `⌘Z` 可撤销。
  没有标题行的卡片可以从右键菜单收 / 展。
- **链接卡：第一次填入链接自动取一次预览**（`O32`）：新建 / 粘贴一张带链接的卡片时会**自动抓一次**
  标题 / 摘要 / 图标 / 最终网址；卡面上**仍然保留「获取预览」按钮**，想重抓随时点（失败会标红并提示重试，
  右键菜单里也有同一项）。「打开」按钮改成了**图标**（分享 / 外链）。★ 自动那一次是"只在你点那一下才联网"的
  **一个例外**（触发点仍是你**粘贴 / 新建那一下**）；关掉总开关时一次请求都不发。
- **`.md` 文件卡改标题会改真实文件名**（`O30`）：指向 Markdown 的**文件卡**改标题时，Vault 里那个文件
  跟着改名（卡片路径自动跟随）；标题为空、或目标已存在同名文件时**不动文件**并提示。
- **分栏**：创建、拖入、插入线落点、栏内重排、折叠展开、整栏移动、`⌘Enter` 批量拆分、多选「收进分栏」。
  - **整栏转编组**（`O04`）：对着分栏右键 →「转成编组」（或框选一栏里**全部**卡片后 `⌘G`），这一栏收成一个组；**原栏不留空壳** —— 栏目录里的成员同时退出，所以标题条与内容盒一起消失。判据认的是**成员集合**（选区恰好等于整栏），框到一半不会把栏拆掉。
  - **空壳栏自动清理**（`O04`）：一栏的卡片**被全部挪走 / 删掉**时，那一栏连壳一起收掉，相邻的右栏自动**合拢**补上空档（位移量按它到邻居的**实际间隙**算，不是标准间距）。新建的空栏不会被误收，清理由撤销栈管 —— `⌘Z` 能把栏和卡片一起找回来。
  - **分栏也能当连线的端点**（`O21`）：从分栏的锚点拖一条线出去、或把线落到分栏上 —— 端点身份从"卡片"放宽到
    "**卡片 ∪ 分栏**"。折叠的栏按**显示高度**取锚点（不会浮在一个看不见的空盒下沿）；拖栏（连同栏内卡片）时线
    实时跟手；**删掉一栏，连着它的线一并清掉**（不留指向虚空的幽灵边）；导入 `.canvas` 时指向 `group` 节点的
    连线也不再被跳过。
- **分组收起**（`O03` / `F5-05`）：分组从"一圈虚线"升级成一个**能收起的容器**。框的左上角常驻一条标签条
  （组名 + 张数 + 收起开关）：
  - 点**收起**，组内卡片**同帧消失** —— 不渲染、不参与框选与命中，连着它们的连线也不画
    （不留一条悬在空气里、指向虚空的线）；屏幕上只留那一条标签。再点一下**原样展开**，
    卡片回到它一直在的地方（收起期间**没动过任何几何**）。
  - 标签条**双击改名**（对着名字双击改名，对着开关双击还是收起 / 展开）；按住标签条拖动，**整组一起走** ——
    网格吸附、智能参考线、拖进分栏、拖出导出、撤销，走的都是卡片拖动那一套。
  - 收起状态写进 `.nboard`（`group.collapsed`，**缺席 = 展开**），收起 / 展开各记一步 `⌘Z`。
    **存量文件读一遍再写回去逐字节不变**：从没收起过的分组里不会凭空多出一个 `collapsed: false`。
  > 收起**不是"隐藏一组卡片"**，而是"这一组现在只占一条标签的位置"。所以标签条**停在原地**
  > （收起是个"缩小"的动作，标签跟着跳一下看起来就像点错了），拖它时带的却是整组。
  > 收起之后这些卡片从选区里也会被摘掉 —— 否则"先选整组、再收起、再按 `Delete`"删掉的是一屏看不见的东西。
- **嵌套与导航**：白板卡进入子板、面包屑、`⌘U` 回父级、`⌘[` / `⌘]` 前进后退。
- **拖放**：Obsidian 文件浏览器、系统文件管理器（`files` 与 `file://` 两条来路）、`obsidian://open?file=…` 内部链接。
- **连线**：卡片锚点拖出连接、颜色 / 实虚 / 箭头样式、自由端、随卡片移动重算。
- **连线四件套**（`T7.10`–`T7.13`）：连线与引用卡这两条最旧的骨架补完了。
  - **Smart 走线**（`F3-03`）：右键一条线 →「走线」→「智能绕开卡片」，线**全程横平竖直地绕开**挡在中间的卡片；
    「直连」回到直线。绕不过去时**退回直线** —— 宁可画一条穿卡的线，也不要"这条线不见了"。
  - **曲线弧度**（`F3-07`）：选中一条直连线，**中点浮现一个小圆点**，拖它把线弯成弧，拖回弦上就是拉直。
    拖动中不改文件、松手一步 `⌘Z` 可撤销，`Esc` 放弃这一拖；右键「拉直」（**只在弯着的时候出现**）。
  - **连线标签**（`F3-06`）：右键 →「标签」→「编辑标签」，文字写在**线的中点**上、垫一块与背景同色的底，
    太长自动截断。画布、PNG、SVG、PDF、打印里都画。
  - **块引用**（`T7.10` / `F10-07`）：引用卡可以只显示笔记里的**某一段** —— 右键 →「引用某一处」，
    选「整篇」/ 某个标题 / 某个块 id（`^blockid`）。目标被删掉或改名时**显示整篇并提示**，
    而不是变成一张空卡（至少你还看得见内容、也知道该去重选）。
  - 这四件事共用**同一个**"线长什么样"的函数（画布 / 命中 / 框选 / PNG / SVG 五处一致，
    于是"看着绕开了、点下去选中的是直线"这类问题从根上没有了）；顺带把箭头的几何修对：
    **尖端落在锚点上、两翼朝着卡片**（多段走线与弧线上的箭头曾指反），**带箭头的一端会把线收短**
    （线不再从箭头底下伸出来一点）。
- **编辑**：撤销重做、编组、对齐工具、吸附网格、智能参考线、卡片锁定。
- **卡片旋转**（`T7.06` / `F2-00-10`）：单选一张卡时，卡片上缘多出一个**旋转手柄**，拖到哪转到哪；按住 `⇧` 吸附到 15°（摆正 / 摆 45° 斜排不用靠手感凑）。旋转**不改卡片的尺寸与位置**（布局框一寸不动），只改姿态 —— 所以转过 90° 的卡片，八个缩放手柄仍然跟手，改过大小也不用重摆。右键菜单给**转过的**卡一项「重置旋转」回正（走正常提交，撤销一步到位）。转过的卡片连同**它上面连线的锚点**一起进 PNG / SVG / PDF 导出；`.canvas` 没有角度这个概念，导出前的损失清单会**明说**这一项（转过的卡在对面是正的）。
- **手绘三支笔**（`T7.08` / `F4-07`、`T7.07` / `F4-06`）：工具栏上，画笔旁边多了**荧光笔**与**临时标注**。
  - **荧光笔**是半透明高亮：同色、线宽 ×4、不透明度 0.35，盖在字上看得清底下的内容；半透明的一笔**不跟压感**（整条一次描边，不会在接缝处叠成深浅斑）。不透明度是笔迹自己的一个可选字段，**普通笔迹不写它**，所以已有白板文件逐字节不变。
  - **临时标注**是"不落盘"的那支笔：`⌘⇧A` 开关（再按一次收起笔），画出来的笔迹只留在屏幕上 —— **不进文件、不进撤销栈、不进导出与缩略图**。`Esc` 一键清空并退出，也可以点工具条上的 ✕ 只清空、接着画；`⌘⇧⌫` 在同时有"落盘笔迹"与"临时笔迹"时**先清临时的那层**。
- **检索**：白板内搜索（`⌘F` / `⌘G`）、按类型过滤、按标签过滤、断链总览。
- **导出**：Markdown、PNG（分页 tile 与单页全景）、PDF（真实多页 tile）、SVG（矢量）、ZIP（白板 + 附件打包）、打印（真实分页 + 海报分幅）、复制为 Markdown、`.canvas`（JSON Canvas 1.0）。
- **右上角「⋯ 更多」菜单**（`O11`）：白板视图右上角多一枚「⋯」按钮，把"这块板做完了接下来会做什么"收在一处 ——
  **导出为 PNG / SVG / PDF / Markdown**、**复制白板链接**、**打开 Home 白板**、**适应全部内容**，七项分三组。
  每一项就是命令面板里那条命令的另一个入口（同一个对话框、同一份行为、同一套快捷键），措辞也**直接复用命令名**，
  从哪儿点读到的字都一样。按钮走自有 DOM，**不依赖 Obsidian 私有的 leaf header API**；移动端 44×44，
  键盘 `Tab` 可达、回车展开、`Esc` 关闭。「复制白板链接」在板子还没加载完时置灰、「适应全部内容」在空板上置灰
  （都没得做），「打开 Home 白板」永不置灰（没设 Home 时它正是告诉你去哪儿设）。
- **`.canvas` 互转**（`T4.11`–`T4.13`）：命令面板或白板右键即可把当前白板导出为 JSON Canvas 1.0
  （`.canvas`），或把库里的 `.canvas` 导入成新白板。**导出前先摊开损失清单**——类型降级
  （待办 / 色板 / 白板 / 手绘 ⇒ 文本卡）、占位文本、悬空连线、折叠分栏、外观修饰逐条列出，
  认定后再写盘；目标文件**顺延命名、绝不覆盖**已有文件。导入不改动源 `.canvas`，新白板落在
  它旁边、同名，随即打开。
- **导出 SVG**（`T6.01` / `F9-06`）：把整块白板导出为一个**矢量**文件，放大多少倍都不糊，
  适合放进 PPT / 打印 / 交给设计。选项只有两个 —— **范围**（整板 / 当前视图 / 仅选中）与
  **背景透明**，因为矢量既没有倍率、也没有分页。**图片卡 / 地图卡 / 手绘卡在 SVG 里只保留文字 /
  占位框，不内嵌位图**（内嵌会撑大文件、也丢了"可缩放"的意义）—— 这是一次**安静**的降级，所以导出前的
  对话框会明说"有几张图片卡只留了文字"，而不是等用户打开文件才发现图没了。
- **导出 ZIP**（`T6.02` / `F9-07`）：把这块板连同它引用的**图片 / 文件附件**打成一个包，
  发给别人或在别处重建时不会满屏断链。归档**保留库内相对路径**（`assets/pic.png` 就叫
  `assets/pic.png`），解压到对方库根即可开板。**不压缩（store 原样存储）**——附件本就以
  PNG / JPEG 为主、已压过一轮，deflate 再压几乎不缩水，代价是包比理论值大一些；
  **只打包附件**，引用的笔记（`noteRef`）与子白板（`boardRef`）不进包（它们留在 `.nboard`
  里仍是链接）。库里找不到的附件会在导出前**单独列出**；打包过程中读不出来的文件会跳过并
  在提示里说明，绝不静默少几份。
- **打印**（`T6.03` / `F9-10`）：命令面板里的「打印白板」把整块板印成**真实的多页 A4** ——
  与「导出为 PDF」**共用同一套分页**（同样两个选项：范围 + 纸张方向），所以印出来的页数与
  每页内容永远和 PDF 一致。跨页时对话框会明说"共 N 页、几行几列"，并提示相邻页边缘**刻意**
  留了一点重叠：全部印出来后按边裁掉、拼起来就是一张大图（**海报分幅**）。
  单页不印页脚（一张纸上的"第 1 / 1 页"是废话），多页才在每页底部标"板名 · 第几页 / 共几页"。
  > 走宿主自己的打印通路（`iframe` + `print()`），**不在库里留下任何文件** —— 临时文件、
  > PDF 中转都不需要。打开打印对话框失败时会单独提示，并**指向「导出为 PDF」**：
  > 那才是用户真正能走通的替代方案，比一句"打印失败"有用。
- **附件内容去重**（`T6.05` / `F2-3-10`）：设置里一个开关（**默认关**），打开后导入的图片 / 文件
  按 **SHA-256 内容**判重，同一份内容只存一份，多处引用共享同一个附件。判重用宿主的
  `SubtleCrypto`，**不引入任何哈希库**；拿不到它的宿主会退成"各存一份"，绝不因为算不出哈希而
  让导入失败。
  > **默认关**是有意的：同一次会话里把同一个文件拖两次常常是**明确的重复动作**（想放在别处、
  > 想改个名字），静默合并成一份会让人以为"第二次没生效"。开关**可逆**（关掉即恢复各存一份），
  > 所以点开时不弹确认框。
- **链接抓取的域名黑名单**（`T6.06` / `F2-4-6`）：在设置里列出一批"不抓取这些站"，插件对它们
  一个请求都不发 —— **网页和预览图都不会**。一行一个站，写域名就够（`example.com`），
  而且是**整站生效、含子域**（`m.example.com` 同样不抓）；粘整条带参数的网址也行，保存时会
  收成域名（`https://x.com/a?utm=1` → `x.com`）。黑名单与总开关是**两个正交的旋钮**：
  总开关管"要不要联网"，黑名单管"哪些站不碰"。这一项**只在总开关打开时出现在面板上** ——
  关着联网的时候它一个请求都拦不到，摆出来就是个怎么填都不生效的输入框。
  > 被拦下的卡片会**明说**这件事（"这个站在你的不抓取列表里 —— 没有发出任何请求"），
  > 而不是混进一句"没能抓到预览"里 —— 后者会让用户以为是网络问题而反复重试。
  > 抓取入口是所有链接卡共用的那一个（按钮与右键菜单同源）。
- **自动整理 / 按标签分栏**（`T6.07` / `T6.08` / `F5-06` / `F5-07`）：画布右键或命令面板里两条
  "一键去混乱"的动作。**自动整理**把分栏、栏外的编组、栏外的散卡按"看不看得见地并排"重排成
  整齐的行列 —— 顶部对齐、间距统一，而**行序与行内次序都跟着"原来在哪"走**（位置才是混乱，
  上下左右的关系是意图，整理不替你重新排序）。**按标签分栏**把带同一个 `#标签` 的散卡收进一个
  以 `#标签` 命名的新分栏（一个标签至少出现两次才开栏）。
  > 两条都**只动"块"的位置，不动块里的东西**：卡片 / 分栏的尺寸、分栏的成员归属、`z`、锁定、
  > 演示顺序一个字节都不改。**锁定的卡片留在原地**（宁可和新版面重叠，也不悄悄挪走它）；
  > 你手划的分栏**一张都不拆**；已经整齐 / 没有可分的标签时**什么都不做**。
  > 归栏规则：一张卡有多个标签时只认第一个（**标题**里的优先于正文里的）；已有同名分栏
  > （`项目` 也算 `#项目`）就收进去、不新建 —— 所以连按两次不会得到一套重复的栏。
  > 标签解析刻意挡住链接锚点（`[x](#a)`）与 markdown 标题（`## …`），纯数字不算标签，
  > 大小写不敏感（显示保留你第一次的写法）。新栏放在既有内容**下方**并自动等宽对齐，
  > 一眼能看出"这是刚生成的"。
- **本地模板市场**（`T6.09` / `F7-06`）：「从模板新建白板」从一行字的列表升级为**看得见样子的画廊**：
  每张模板一张**真几何缩略图**（与 PNG 导出、板缩略图同一套渲染，只缩不放），下面配名字与说明；
  顶部一个搜索框、一排分类切换（全部 / 调研 / 排期 / 视觉 / 写作 / 我的模板），点一下即建板。
  > **预览是"真板子的缩小"，不是示意图** —— 缩略图用 `boardThumb` 的同一套几何与配色，
  > 所以你在市场里看到的样子，就是新建出来的样子；为此**不会**为了做预览去 decode 大图
  > （图片卡画成占位块），打开市场不会因为库里存了几张 4K 图而变卡。
  > 分组与搜索都是纯逻辑（`model/templateCatalog.ts`），内置按分类成组、你的模板自成一组、
  > 搜索同时匹配名称 / 说明 / 路径（"我放在 `调研/` 里的那份"也搜得到）。模板多到几百份时
  > 只先画前 100 张并提示你去搜索 —— 一次性建太多画布会把窗口拖卡。
- **拖出卡片 → 导出为笔记**（`T6.10` / `F6-04`）：把卡片拖到左侧**文件浏览器**的某个文件夹（或某个
  文件所在的那一格）上松手，就把这几张卡导出成 `.md` 笔记写进去。拖动过程中指针底下的文件夹会
  **亮起描边**告诉你"会落在这儿"。一次拖的卡里空的会被跳过；全都没内容时给一句提示，**不会**写出一堆空文件。
  > **复用移动手势，而不是浏览器的原生拖拽**：一开原生 `dragstart`，浏览器会给正在拖的那个指针补发
  > `pointercancel`，把"拖动卡片"这个主手势直接打断。于是反过来用 —— 照常拖，只在**松手那一刻**看指针
  > 落在了哪儿。好处不只是不打架：拖到一半反悔（拖回画布内松手）天然就是**取消**，卡片留在原地。
  > 文件名沿用「提升为笔记」的命名规则（重名**顺延、绝不覆盖**），导出后卡片**不动**（复制出去，不是搬走）。
- **拖一段文字进来 → 便签卡**（`T6.11` / `F6-07`）：在笔记里选中一段文字，直接拖到白板上松手，
  就落成一张**便签卡**，正文原样存进去。多选标签、代码块、整段列表都能这样搬。
  > **原来拖外链是"什么都不发生"，这一点没变**：整段选中的恰好只是一个网址（`https://…`、`mailto:…`）
  > 时仍然什么都不做，不会被拆成一张只写着网址的卡；空选区、只拖了换行也不会凭空多出一张空卡。
  > 这条判定排在文件浏览器 / 库内链接 / 系统文件之后，所以前三种既有拖放的**行为一个字节都没变**。
- **演示模式**（`J-06` / `J-07`）：边讲边翻卡片。`⌘⇧P`（或画布右键 →「开始演示」）进入，
  `→` / `←`（`Space` / `Backspace` 同效）翻步、`1`~`9` 跳步、`O` 总览整块板、`Esc` 退出；
  底部一条步骤条显示"第几步 / 共几步"，当前卡描边高亮、其余卡片淡化。相机**平滑飞过去**
  而不是瞬移，飞行途中一按指针立刻停在原地。
  **讲哪些卡**：排过顺序（卡片右键 →「加入演示路径」，可多选）就只讲那几张、按你排的顺序；
  一张都没排过则按**阅读顺序**讲全部卡片——零配置也能直接开讲。顺序写进 `.nboard`（跟着文件
  与 Git 走，可 `⌘Z` 撤销），顺序调整是「前移 / 后移一步」，每次改动重编号成连续的 `1..N`。
  **演示时不可编辑**（硬约束）：拖动、双击编辑、空白双击新建、卡片右键、`Delete`、`⌘D`、
  `D`（画笔）以及命令面板里的编辑类命令全部失效，但画布仍可平移缩放（滚轮 / 触控板 / 中键拖动 /
  演示态下空白处左键拖动）。系统开了「减少动态效果」时，切步是直接跳过去。
  > 「阅读顺序」的定义是**按块从上到下、从左到右**，**一个分栏算一整块**：走进去就把那栏讲完
  > 再走下一栏（栏内按栏内顺序），压在分栏上方的标题卡会先讲。让分栏逐张参与全局排序会得到
  > "左栏第 1 张 → 右栏第 1 张 → 左栏第 2 张……"这种左右横跳的顺序 —— 讲一块排好版的板子时，
  > 那正是最反直觉的结果。想穿插着讲就用「加入演示路径」排一遍。
  >
  > 键位调整：`⌘⇧P` 从「提升为笔记」**让给了演示**（`04 §9` 的规定）。那条命令仍在命令面板里，
  > 想要快捷键可在 `设置 → 快捷键` 自绑。
  >
  > 进演示时**先收掉"正在编辑的卡片"与"正在画的笔"**：演示态关掉的全是入口，已经开着的编辑器
  > 或手绘层照样收字 / 收笔画 —— 不收掉就会出现"说好不能编辑，一敲键盘字还是进去了"。
  >
  > 演示态的 `Space` 是"下一步"（`04 §9`），所以 `Space` + 拖动**不再**是平移手柄；
  > 与之配套，演示态**空白处左键拖动**就是平移（那时框选本来被封着，空白处一拖什么都不发生）——
  > 鼠标用户不必为了挪画布去按住一个会翻页的键。普通浏览态的空白拖动**仍然**是框选。
- **跨白板复制 / 剪切 / 粘贴**（`T4.15` / `F7-07`）：选中卡片 `⌘C` / `⌘X`，在**任何一块白板**上
  `⌘V` 就能粘出来（插件 `02 §4.1` 的键位表早就定的组合）；卡片右键菜单也加了「复制 / 剪切」。
  剪贴板里放的是一段**文本**，所以跨白板只是它最小的一件事 —— 跨窗口、跨库、跨这次 Obsidian
  会话都成立（把那段文本贴进任何文本框还能读出来）。
  > **搬运规则**（每条都有单测）：**连线**只搬"两端都在选中集里"的 —— 一端在外面的连线搬过去
  > 就是一条指向空白处的线；粘出来的卡片一律是**散卡**（目标板没有同一个分栏，想归栏粘完再拖）；
  > **演示步骤号清空**（演示路径是目标板自己的脚本，复制进来的卡不该挤进别人的讲解顺序）；
  > 内容深拷贝、id 全新、相对位置保持、整组按包围盒左上角对齐粘贴点。
  >
  > 剪切 = **复制成功之后**才删当前板的卡，删除走正常编辑历史（可 `⌘Z` 撤销）；
  > 写剪贴板失败就**不删** —— 不能让一次剪切把卡片弄丢。
  >
  > 只读板（归档锁定）上的「复制」仍然可用：它写的是剪贴板，`.nboard` 一个字节都不动。
  > 顺带堵上一个缺口：**演示态下粘贴不落卡**（粘贴是按"内容"进来的，而演示态此前只关了
  > 指针 / 画布按键 / 命令面板这三道闸）。
- **嵌入**：笔记内 `nestboard` 代码块 / `![[x.nboard]]` 只读渲染。
- **集成**：白板内嵌设置面板、中英双语（跟随 Obsidian 语言）、诊断信息面板。
- **数据安全**：`vault.process` 原子写、`revision` 冲突检测、解析失败进只读保护态、失焦 / 卸载强制 flush。
- **版本快照**（`F11-11`）：每编辑满五分钟自动存一份（每块板最多 50 份 / 20MB），也可用「创建快照」手动存；
  「查看历史版本」支持列表（时间 + 卡片数差异）/ 预览 / 恢复，**恢复前会自动给当前版本留一份退路**。
  存放位置可在设置中选插件目录（默认，不进库）或库内 `.nestboard-history/`（随 Obsidian Sync / Git 走）。
- **删除走回收站**：删除白板（命令 + 文件右键）会先自动存一份快照、关掉该板的画布，再移入系统回收站
  —— 不是永久删除。破坏性操作一律二次确认，危险的确认键默认**不**获得焦点（回车 = 不删）。
- **同步冲突副本**（`T4.03`）：识别 Obsidian Sync / iCloud / Syncthing / Dropbox 产生的冲突副本
  （`.conflict-*`、`.sync-conflict-*`、`(conflicted copy …)`、`(case conflict)` 等命名），
  并在启动时、以及副本刚出现时提示。「查看同步冲突」提供**并排只读对比**：按卡片 id 逐行对齐，
  标出「仅当前有 / 仅副本有 / 有差异」，可切换副本、只看差异、打开副本或把副本移入回收站。
  **刻意不提供自动合并** —— 两份分叉的白板里任何自动取舍都会静默丢一边的改动（详见
  `src/ui/modals/ConflictMergeModal.ts` 文件头）。
- **修复引用**（`T4.07`）：把白板复制 / 移动到另一个 Vault 之后，卡片里存的相对路径全都指不到
  文件了（而文件还在，只是换了目录）。命令面板里的「修复引用」会按**文件名**找出可疑的新位置，
  排成一份清单让你逐条确认：「同名」档位（连大小写都一样）默认勾上，其余只是名字相近的猜测
  一律留空。图片 / 引用 / 白板卡各自只接自己那类文件，同名文件多于一个时会提示"不是唯一解"。
  整次修复是一步可撤销的改动 —— 发现选错了，一次撤销就能全部退回。
- **整理未使用附件**（`T4.05`）：命令面板里的「整理未使用附件」会列出附件目录里
  **没人引用**的文件 —— 既没有卡片引用、也没有别的白板或笔记引用。**插件永远不会替你删除**，
  清单每一项只能「打开」：它看不见所有引用方式（笔记正文里的图片、DataViz 之类的插件、
  纯文本），所以删不删由你决定。已不再使用但别处还在用的文件不会被列进来，只报个数。
- **白板 URI**（`T5.06` / `F10-06`）：`obsidian://nestboard?file=…&card=…` —— 把**某块白板里的某张卡**
  变成一个可以写进任何笔记、任务列表、日程、甚至库外 App 的地址。这是「双链深化」里唯一一条
  能让**库外**的东西指回库内的通路（其余都是库内互指）。`card` 可省（= 只打开那块板，不动视口）。
  > 拒绝的原因分三种、各给一句提示：没写 `file`（地址写残了）、指向的不是白板
  > （`.md` / `.canvas` —— 这不是笔误，是**指错东西了**）、路径里有 `..`（试图跳出库）。
  > 宽容面照收：`?file=Boards/我的板` 这种**省扩展名 / 反斜杠 / 多打前导 `/` / 带前后空白**的写法
  > 都能用 —— 它与设置里的 Home 路径**共用同一套归一化规则**（`util/boardPath.ts`），
  > 所以不会出现"设置里能填、链接里不行"。
  > 找不到那块板时**只报路径、不开空标签页**：用户手里只有一个坏链接，他要知道的正是"它指的是哪块板"。
  > 参数由宿主解码过一次，插件**不做第二次解码**（库里真有可能存在名叫 `100%完成.nboard` 的板）。
- **Home 白板与收件箱**（`T5.07` / `F7-03` / `F11-09`）：把"往白板里放点东西"从"每次都要答一个问题"
  降级成**"默认有个答案"**。「打开 Home 白板」不必先想清楚去哪；「把当前文件放进收件箱」不必挑目标
  —— 落进 Home 的 `Unsorted` 一栏，回头再整理（笔记右键菜单里也有这一条）。
  路径在设置里填（**Home 白板路径**），**留空 = 关掉**这个功能：这时两条命令只提示"去设置里填路径"，
  文件菜单里那一项**根本不出现**。
  > **安全底线**：Home 那块板不在索引里时，**先看盘上有没有** —— 有就只登记、**绝不复写**
  > （否则会把用户已经摆好东西的板整块冲掉，这是本功能唯一可能造成数据损失的路径）；
  > 盘上也没有时才按你填的路径（`exactPath`）建一块新的，带一栏收件箱。
  > 收件箱那一栏按**标题**认（标题本来就写在文件里），所以把它改名后"默认落点"会退回视口中心 ——
  > 这是可接受的降级，比"插件偷偷记住一个早被删掉的 id"要好。
  > 新卡片的落点由**视图**算（只有它手上才有分栏几何），调用方只问一句"放进收件箱"。
- **白板列表侧栏**（`T5.08` / `F7-04`）：白板一多，"我那块板呢"就成了第一个卡住人的问题 ——
  而白板的组织方式往往**不是**目录（同一批板可能散在几个目录里，按主题连在一起）。
  侧栏给同一份白板**三种看法**：**目录树**（"我知道它在哪个文件夹"）/ **按最近打开**
  （"我刚才还在看它"）/ **按标签**（"那些 `#项目` 的板"），外加一个筛选框。
  默认键 `⌘⇧B`（也可以从命令面板搜 `Nestboard: 打开白板列表`）—— 这是本插件里少数几个
  **占默认键**的命令之一：它要解决的正是"我找不到东西了"，那一刻不该还要先想"这个功能绑没绑键"。
  > 三种看法都只是"同一份白板换个方式排"，所以排序/分组全在 `ui/boardList.ts` 里，
  > 是零 `obsidian` 依赖的纯逻辑。几个刻意的取舍：**空目录不显示**（只沿有白板的路径建节点）；
  > **筛选命中为空时不画空树**（一个空树只让人以为坏了）；**筛选词不会被记住**
  > （恢复一个被遗忘的筛选器，会让人打开侧栏看见空白列表而不知道为什么），
  > 但**你看的是哪种排法会被记住**。
  > 「最近打开」**不是真相、只是历史**：里面的路径早被改名/删除是常态，所以渲染时逐条回索引核对，
  > 查不到就静默跳过；白板改名时那条路径也会跟着搬走（不搬的话它不是"变成旧名字"，
  > 而是**凭空消失**）。它记在 `data.json` 里，**上限 20 条**（唯一一个由插件自己写的设置字段）。
- **缩略图导航器**（`T5.09` / `F1-06`）：画布**右下角**的一张可点击 / 可拖动的小地图，
  整块板上有什么、你现在看的是哪一块，一眼就有 —— 大板子上"我那张卡到底在哪"不必再靠滚轮找。
  **点击或拖动地图**即把视口中心挪到对应位置（拖住不放实时跟手）；**在地图上按 `Enter`**
  回到全部内容的中心（与 `⌘0` 适应内容同一个归宿）；面板右上角的 `×` 收起它。
  **默认关闭** —— 它是一块常驻浮层，占不占画布该由你点头；命令面板搜
  `Nestboard: 切换缩略图导航器`、或 设置 → Nestboard → 缩略图导航器 打开，
  三处入口改的是**同一份设置**（关掉之后新开 / 重开的白板也不会自己冒出来）。
  > 地图上**不画连线、不画选中态**：连线没有面积，在 176px 宽的地图上画几百条只是噪点；
  > 选中态由画布上那圈描边回答，这里再画一遍只会更糊。
  > **相机跑到内容之外时视口框整块消失**（被地图裁掉）—— 这正是要传达的信息（"我不在任何
  > 内容附近"），画一个贴在边上的假框反而会让人误以为自己正看着某片内容。
  > 几何全部收在零 `obsidian` 依赖的 `ui/minimapGeometry.ts` 里（**等比缩放、内容居中、
  > 每格至少 1px**），所以"地图上点一下等于跳到哪"是**能单测**的；面板本身只管 DOM 与指针。
  > 移动端会把它抬到底部工具条上方，演示模式下自动收起。
- **白板级只读 / 归档锁定**（`T4.06`）：「锁定白板（只读）」命令或画布右键即可把一块板锁成只读
  （状态存在 `.nboard` 自己的 `settings.readOnly` 里，跟着文件走）。锁上之后会改内容的一切入口
  ——工具条、右键菜单、快捷键、双击编辑、手绘、连线、粘贴、拖入——全部失效，
  屏幕左上角出现一条提示与一个「解锁」按钮。**只读不等于断掉一切**：打开源笔记 / 跳到子板 /
  拉预览 / 搜索 / 导出照样可用，卡片菜单里只是灰掉写操作的项。视口仍可自由平移缩放，
  但不再写回文件（避免"只是翻了翻"在多设备上产生噪声同步）。
- **导出 PDF**（`T4.10` / `F9-03`）：整块白板切成**真实的多页 A4** —— 每页都是满幅的一块 tile，
  用任何 PDF 阅读器翻页、打印、发给别人。纸张可选纵向 / 横向，清晰度三档
  （每页**长边** 2048 / 3072 / 4096 px；**只改印刷清晰度与体积，页数不变**），
  页码与白板名印在每页页脚。
  > **产物里的文字是位图**：不能选中、不能搜索 —— 要可复制 / 可检索的文字请用「导出为 Markdown」。
  > PDF 每页是一张 JPEG（`/DCTDecode` 直通，**不重编码**），要可搜索的文字就得嵌字体子集，
  > 那是另一个量级的工程（中文字体动辄几 MB）。
  >
  > **没有引入任何 PDF 库**：`src/export/toPdf.ts` 自己拼 PDF 1.4 —— 对象 / 内容流 / 交叉引用表
  > 逐字节写出，因此也没有"嵌字体"这条路。分页规则与 PNG 的 tile 同源
  > （按**纸面**反推每页装多少世界坐标，最后一列 / 行贴回右 / 下边缘：宁可两页重叠一点，
  > 也不留一条细长的半页），所以两种导出的拼贴几何是一致的。
  >
  > 与 PNG 导出的两处刻意差异：**没有"单页全景"**（整块板压进一张 A4，字会小到印出来看不清）、
  > **没有"背景透明"**（PDF 里是 JPEG，没有 alpha 通道）。
- **模板库**（`T4.14` / `F7-06`）：不必每次都从空白板开始。命令面板「从模板新建白板」列出
  **4 个内置模板** —— 桌面研究 / 每周排期 / 情绪板 / 长文骨架（每个都带一张说明卡说清怎么用），
  以及**你自己的模板**（设置里「模板目录」下的 `.nboard`，认子目录，可以按主题分文件夹）；
  「另存为模板」把**眼前这块板**复制一份进模板目录，同名**顺延编号、绝不覆盖**。

  > 模板本身就是一份普通的 `.nboard` 文件 —— 用户看得见、能用别的编辑器打开改、能进 Git、
  > 能直接发给同事，也不用插件再长一套导入导出。代价是它也会出现在白板选择器 / 注册表里
  > （见「已知限制」第 9 条），这是刻意的取舍，不是疏忽。
  >
  > **用模板建板 = 换身份，不换内容**：所有 id 重新生成，并**同步重写引用它们的字段**
  > （卡片归属的分栏、连线的两端、编组的成员）—— 漏掉一处，用户拿到的就是一块连线飘在空中、
  > 卡片不属于任何分栏的板；视口落回原点，清掉只读与别名，`parent` 按入口归位。
  > 三者**刻意保留**：画布背景（模板的样子是模板的一部分，不会被设置里的默认背景顶掉）、
  > 其余 `settings`（网格吸附之类是模板作者调好的）、卡片演示步骤（模板自带的讲解顺序）。
  > 新板落在设置里的「新白板目录」（不是模板目录），标题取模板名，别名为空，建完随即打开。
  >
  > 中文 / 英文两套内置模板：模板里的标题、正文、分栏名都是**用户能看见的字**，
  > 所以和界面文案一样走 i18n —— 英文环境下新建出来的板不该是一堆中文提示。

- **卡片编辑流：先写标题，再写正文**（`O01`）：双击一张**标题行开着**的便签卡 / 同步便签卡，
  先出现的是**标题输入框**（原名已选中，直接打字就覆盖），`Enter` / `Tab` 落到正文编辑，
  `Esc` 则放弃标题、回到进入之前的样子。右键「编辑标题」保持老语义（**只改名字**，写完就收工）。
  `⌘` + 双击 与"右键 → 编辑内容"**跳过标题那一步**直接给正文。
  > 名字是卡片的"标签"：摆一屏卡片时先看见的就是它，而此前给它起名字只能先去右键菜单。
  > 新建出来的卡片**默认不显示标题行**，所以"落卡即打字"这条老习惯一步没变 ——
  > 这一步只在标题行开着时出现，关掉那一行的人不会被塞一个标题框。
  >
  > 顺带把**卡片标题的字重与字号抬了一档**（1.12 × 正文字号、半粗、字距略开）：缩到 30% 看一整块板时，
  > 卡名此前和正文糊成一片。字号跟着设置里的正文字号走，改大正文时标题不会原地不动。
- **待办卡：标题与清单分两格**（`O02`）：`TodoContent.title` 是模型里的一等字段，
  以前却只能靠在源码里手写 `# 标题` 去改 —— 手滑删掉那个 `#`，标题就变成一条待办项。
  现在双击待办卡是**上下两格**：上面标题框、下面清单（`[ ] ` 变 `- [ ] `、`Enter` 续行、`Tab` 缩进照旧），
  光标先落在标题上，`Enter` 落到清单。
  > **两格只在整次编辑结束时合成一次写入**：`updateContent` 的既有约定是"写入 = 这次编辑到此为止"，
  > 每敲一格就写一次的话，用户点一下标题就会看见清单消失。于是标题先在 DOM 里待着，
  > 由"焦点离开卡片"（两格一起提）、"清单里 `⌘Enter` / `Esc`"（连标题一起提）、
  > "标题框里 `Esc`"（只提清单）三条路收口，各只写一次、只记一步 `⌘Z`。
  > 焦点只是在两格之间挪（清单 → 标题）不算结束。
  > `⌘` + 双击 / 右键「编辑内容」仍是**老入口**：整份源码（`# 标题` + `- [ ] 项`）进一块输入框。
- **文件树右键菜单分组 + 「在此新建白板」**（`O12`）：菜单项按「当白板用 / 当卡片用 / 维护」三组排开，
  组间有分隔线；对着**文件夹**右键多了一项**「在此新建白板」**，新板就落在那个文件夹里
  （文件名、重名顺延、建完打开与 `⌘⇧N` 完全同源）。
  > 排布搬进了 `view/interact/fileMenu.ts`：Obsidian 的 `file-menu` 只能在真实工作区里触发，
  > 摊在 `main.ts` 的回调里的话，"文件夹有没有这一项""冲突副本是不是独占""分隔线落在哪"只能靠人眼点一遍。
  > 动作表是 `Record<FileMenuAction, …>` 而不是 `switch` —— 漏一个动作**编译不过**。
  > 同步冲突副本仍**独占**整份菜单（只有「对比」一项）：它是唯一还留着冲突内容的文件。
  > **文件树的空白处没有入口**（Obsidian 没把那个位置的右键开放给插件，见说明书 §17）。

### 修复

- **Markdown 里的链接点不动**（白板卡片与脑图节点都受影响）：`MarkdownRenderer` 只负责把
  `<a class="internal-link" data-href="…">` **渲染出来**，"点它跳到哪"那一步是**视图**的事，
  宿主对自定义视图不兜底 —— 症状是链接看着像链接（有样式、有 `data-href`），点下去什么都不发生。
  现在两边共用同一个助手（`integration/markdownLinks.ts`）：内部链接交给 `openLinkText`
  （带 `#小节` / `|别名` 的写法都认），**开在新标签页**（不把正在看的板 / 脑图顶掉），
  外部链接交给系统浏览器；`#标签` 与嵌入块一概不动。
- **脑图导出的节点没有边框**：屏幕上节点有"发丝边 + 两层影子"，而导出只画了矩形 ——
  同层的白底节点在导出图上会**糊成一片**。现在导出的每个节点都带一道**发丝边框**
  （压在标题带之上）并挂一层 `feDropShadow` 阴影；边框是最稳的那一道：不依赖渲染器支持滤镜，
  在缩放、打印、别人转格式之后都还在。

- **换成迷你形态后，白板卡换图标"点了没反应"**（`O38`）：快捷操作栏给白板卡写的标记落在**卡级** `icon`，
  而白板卡的标记一直存在**内容里**（`BoardRefContent.icon`，`O10` 起就是这样 —— 迷你档正中那一格、
  展开档标题行左边那一格都读它），于是"写进去了、渲染读的是另一处"。现在读写都按**卡片类型**分派
  （`cards/cardIcon.ts` 的 `cardIconOf`）：白板卡走它自己的写入口（归成内容、一步 `⌘Z`），便签照旧。
  右键「更换图标」与快捷栏那一格两条路一起修好；导出的 PNG 也一并跟上（之前会漏掉白板卡的记号）。
- **"标题 = 文件名"的卡片改标题会改真实文件**（`O37` / `O38`）：**白板卡**（卡面上的名字一直取自那块
  `.nboard` 的**文件名** —— 标题行与迷你形态卡外那行都是）与**引用卡**（卡面第一行 `nestboard-note-ref-name`
  也是**笔记文件名**）此前都只写卡片自己的标题字段、不碰文件，于是"打完字卡面没变、文件也没动"，
  看起来像坏了。现在这两类卡的「编辑标题」**就是改文件名**：Vault 里那块板 / 那篇笔记真的改名、
  卡片路径自动跟随（不断链），白板卡的展开形态与迷你形态同时生效；输入框里预填**当前名字**，直接改就行。
  只对**白板卡 / 引用卡 / `.md` 文件卡**（`O30`）生效 —— 别的附件卡（PDF / 表格 / 图片）改标题不动文件；
  空名字 / 只剩非法字符 / 已有同名文件时只改卡片上的字并提示，绝不覆盖别人的文件。
- **收起的色板卡变成一条白条**（`O33`）：这一格色画在内容槽里（底色层是它的孩子），而收起
  会把整个内容槽藏起来 —— 颜色是色卡**唯一**的信息，一收起就没了。现在收起时这一格色改由
  **卡片外壳**自己画（`syncShellSurface` 写变量 + 样式表铺色），纯色 / 渐变都对，展开后与原来一致。
- **收起的链接卡那一行是空白的**（`O34`）：收起只留标题行，可它写的是卡片自己的 `title`，
  而链接卡的名字一直住在预览抓到的 `content.title` 里。现在收起时那一行给**网页标题**；
  没抓到预览就给**链接本身**；你自己写过标题的仍是你的标题。顺带：**收起时标题行与 `▸` 一定在**
  （此前关掉标题行的卡片一收起，连展开按钮都被藏掉，只能靠右键菜单展开）。
- **「编辑标题」点了没反应**（`O36`）：标题为空的卡片（文件 / 链接 / 色板 / 图片 / 地图…）上，
  空标题会把**整行**藏起来 —— 输入框于是挂在一个看不见的行里。现在进入标题编辑时会把那一行放回来。
- **右键菜单里会"跳转"的「编辑内容」**（`O35`）：文件 / 链接 / 图片 / 地图 / 色板 / 白板 / 引用卡
  的双击都是"打开它指向的东西"，这一项点下去其实是**跳转**（引用卡上还与它自己的「编辑内容」
  重名成两条）。现在这些类型的「编辑内容」**整项不出现**，各自的真入口照旧在（「用系统默认应用打开」/
  「在浏览器中打开」/「打开白板」/「打开源笔记」…）；便签 / 待办 / 同步便签 / 评论 / 手绘的照旧保留，
  色卡则保留一条能改**色号文本**的（双击色卡仍是弹调色板）。
- **收起卡片后，连着它的线不动、线头还偏了一截**（`O31`）：绘制（`EdgeRenderer`）与命中 / 锚点
  （`BoardView.cardRectLookup`）两张卡片矩形表都用的是模型的 `height`，而收起只改**显示高度** ⇒
  线看不出变化、端点落在卡片外面。两处现在与骨架同源（`cardDisplayHeight`）。
- **便签显示态的正文贴着卡片边框**：非编辑态正文补上内边距（12px），文字四周留出余地、不再贴边
  （编辑态不受影响，仍是整块编辑区）。
- **便签双击进编辑后没有"标题那一格"**（`O22`）：便签此前靠"先弹标题行输入框、再进正文"，
  而这一步依赖**标题行开着**（关掉标题行的便签得不到它）且**卡片已挂载**（"双击空白新建便签"
  是落卡即编辑、那一刻卡片还没挂载，于是被**静默跳过**、直接落进正文）。现在编辑态本身就是
  "标题框 + 正文框"两格，不再依赖标题行、也不依赖挂载；关掉标题行只影响**显示态**顶部那行名字。
- **白板改名后，面包屑还挂着旧名**（`O15`）：面包屑每一级取的是白板文件内部的 `meta.title`，
  而那个字段**只在建板时写过一次**（`io/newBoard.ts`），此后的改名 / 移动都不会更新它 ——
  于是"白板卡上是新名、顶上还挂着旧名"。现在每一级**一律显示文件名**（与白板卡同一口径），
  并且改名**当场**重画面包屑（`retargetPath` 里补一下，不必重开标签页）。
- **分栏收起后，栏内卡片还留在画布上**（`O16`）：收起原本只把分栏自己压成 40px 的标题栏，
  而栏内卡片是分栏的**兄弟节点**、由卡片层统一渲染 —— 卡片层不知道分栏收起了，
  于是"折叠"之后一栏卡片照旧浮着。现在收起即把栏内卡片并入"屏幕上不存在的卡片"：
  不渲染、框选框不到、命中与连线也当它们不在；展开原样回来，位置不会错乱。
- **「新建地图卡」先弹库内文件选择器**（`O17`）：用户的心智是"地图卡 = 粘一条地图分享链接"，
  而 `O08` 做的粘贴只挂在**已存在卡片**的右键菜单上，与新建入口对不上，于是"点地图卡却让我选文件"。
  现在新建地图卡**落卡后直接问链接**：剪贴板里就是地图链接就直接粘上（全程没有弹窗），
  读不到或认不出才弹输入框；**取消则把刚落的空卡撤掉**，画布上不留空壳。
  想拿一张库内图片当地图，右键「选择地图图片」照旧（换图保留图钉）。
- **白板卡的「迷你」档不像迷你**（`O18`）：这一档此前是"再少两行字的缩略图"—— 卡面上仍画那张
  256px 缩略图（等比放大裁切），尺寸还是你自己拉的，于是三条反馈一起冒出来：**它仍在预览内容**、
  **设了图标看不到**（图标画在被藏掉的标题行里）、**名字没有地方显示**。现在这一档改成
  **不预览内容的固定正方形**：正中只放这张卡的 emoji 图标（没设图标就是主题色小方块），
  **板名画在方格子正下方居中**，边长固定为便签默认宽的三分之一（约 87px）——
  选中它时**不再出现八个缩放点**（**旋转**不受影响）。切到这一档会当场把卡钉成正方形、
  切走时还回默认尺寸（280×180），一步 `⌘Z` 可撤销；`O09` 时代拉成别的尺寸的旧卡片，
  打开白板时会被自动掰正（这是唯一会改卡片尺寸的档位）。
- **评论卡刚建出来就能拖**（`O13`）：新建评论卡曾**自动进入编辑态**，而编辑态里的输入框一上来就吃住指针 ——
  卡片刚落到画布上就拖不动，得先点一下别处退出编辑才能挪。现在**落卡一律停在显示态**：
  想写点什么，点一下「写一条备注」或双击卡片。这条规矩归**卡片类型自己**管
  （`CardTypeDefinition.autoEditOnCreate`，当前只有评论卡写了 `false`），便签 / 待办等照旧落卡即编辑。
- **拖入的笔记不再被改名**（`O14`）：从库外（Finder / 资源管理器）拖进来的 `.md` / `.markdown`
  曾和其它附件走同一条导入路径，文件名被套上 `20260913-153012-` 这种时间戳前缀 ——
  一篇「会议纪要.md」落库后变成一串数字，引用卡上显示的也是这串数字。
  现在按**"是不是笔记"分流**（`isNotePath`）：**笔记保留原文件名**，
  图片 / PDF 等附件仍加时间戳前缀防重名（系统里叫 `image.png` 的文件太多，不加就在附件目录里互相覆盖）。
- **输入法组词中的 `Enter` 不再被当成"写完了"**（`O01` / `O02`）：中文输入法确认候选词用的就是 `Enter`，
  而标题输入框（便签 / 同步便签的标题行、待办卡的标题框）此前只拦了 `Escape` ——
  选字选到一半就被当成"标题写完了"。这一步以前最多是**提前提交**，现在还会借"接着往下"
  把光标推进正文 / 清单，所以两道闸门都补上了 `isComposing || keyCode === 229` 的放行。
- **分栏里的卡片收起后会"腾地方"**（`O05`）：分组收起后，分栏的**栏内位置与栏标题上的张数**
  此前拿的都是模型里的全部卡片 —— 于是那一栏会**空出一截**、标题上的数字也还算着看不见的卡。
  现在栏里所有"数得着的"地方都只算**看得见**的卡片（同一个判据 `collapsedCardIds`），
  收起的卡**既不占位也不计数**，栏的底边也跟着内容收缩。
  > 收缩只收**"内容少掉的那一截"**：你自己把栏拖高留出的余量保留，不会"啪"地缩回拖之前的高度。
  > 栏里被收起的卡片展开后**原地归位** —— 收起期间它的坐标一直留着，只是不再推着栏里的堆叠位次走。
  > 一次改动里"有栏该长、有栏该缩"时先长后缩，两边按**同一份**改动前的基准算。

### 性能基线（T1.75 / T2.19）

记录口径：**自动化部分**用 `npm run perf` 重跑；**帧率与内存类**必须在 Obsidian 桌面端按 `02 §8.4` 人工测，
脚本不生成这两类数字（编出来的基线会让后续每次回归都拿假数据比较）。

#### 自动化基线

环境：Node v26.5.1 · darwin arm64 · 2026-09-12 · 取样总耗时 0.3s

| 取样项（中位数）   | 100 卡  | 1000 卡 | 5000 卡 | 10000 卡 |
| ------------------ | ------- | ------- | ------- | -------- |
| 生成               | 0.166ms | 1.30ms  | 3.75ms  | —        |
| 序列化（自动保存） | 0.099ms | 1.09ms  | 4.75ms  | —        |
| 解析建模（打开）   | 0.167ms | 1.39ms  | 6.23ms  | —        |
| 搜索               | —       | 0.465ms | 1.63ms  | 2.50ms   |

文件字节数（对应 `02 §8.1` 的「文件字节」）：

| 规模    | 字节    | 大小    |
| ------- | ------- | ------- |
| 100 卡  | 60681   | 59.3KB  |
| 1000 卡 | 596701  | 582.7KB |
| 5000 卡 | 2994323 | 2.86MB  |

与 `02 §8.1` 的对照：

| 指标                          | §8.1 目标 | 实测       | 口径说明                                    |
| ----------------------------- | --------- | ---------- | ------------------------------------------- |
| 打开 1000 卡（文件→内存模型） | ≤ 800ms   | **1.39ms** | 只含解析 + 迁移 + 规范化，**不含 DOM 渲染** |
| 自动保存 1000 卡文件          | ≤ 120ms   | **1.09ms** | 只含序列化，**不含 `vault.process` 写盘**   |
| 白板内搜索（10k 卡片文本）    | ≤ 200ms   | **2.50ms** | 全量扫描 + 排序，接近完整路径               |
| 内存增量（1000 卡）           | ≤ 250MB   | **1.8MB**  | Node 堆增量（已强制 GC）；渲染进程内存另测  |

#### 桌面端人工项（待补）

以下指标依赖真实 DOM、合成器与 Obsidian 启动流程，**当前尚未实测**，不计入达标：

| 指标                          | §8.1 目标 | 测量方法                              | 状态 |
| ----------------------------- | --------- | ------------------------------------- | ---- |
| 插件加载时间（`onload`→可用） | ≤ 150ms   | DevTools Performance 录制冷启动       | 待测 |
| 打开 1000 卡（文件→可交互）   | ≤ 800ms   | 打开 `benchmark-1000.nboard` 并录制   | 待测 |
| 拖动卡片帧率（1000 卡）       | ≥ 55fps   | DevTools FPS meter，拖动 10s          | 待测 |
| 缩放画布帧率（1000 卡）       | ≥ 50fps   | 同上，滚轮缩放 10s                    | 待测 |
| 绘制笔画延迟（指针→渲染）     | ≤ 30ms    | 手绘模式画一笔，看 Performance 时间轴 | 待测 |
| 单键输入响应（编辑态）        | ≤ 50ms    | 便签编辑态快速输入，看输入延迟        | 待测 |
| 内存增量（1000 卡，渲染进程） | ≤ 250MB   | 插件诊断面板读数                      | 待测 |
| 打开 50 个白板标签后内存增长  | ≤ 100MB   | 插件诊断面板读数                      | 待测 |

> 生成 / 序列化 / 解析 / 搜索的数字包含**全部 12 种卡片类型、分栏、连线与编组**，
> 由 `src/tests/benchmark/benchmarkBoard.ts` 确定性生成，可直接 `git diff` 比对两次运行。

### 已知限制

- 内联卡（便签卡）里的 `[[链接]]` 不参与 Obsidian 全局图谱；跨白板反链在 v1.1.0 提供。
- 会发起网络请求的功能只有两处，且**都不会自己发**：**链接预览抓取**（`O20` 起**默认开着**，但只有你在卡片上
  点「获取预览」那一下才发一次请求；开启后可另外用**域名黑名单**把某些站整个排除掉，连预览图都不下载）与
  **地图卡静态图服务**（默认「不出图」，四档里挑一家才会在用户点「粘贴地图链接」时发一次请求）。
  渲染白板时一次都不联网。
- 地图卡的分享链接**不跟随短链**：`maps.app.goo.gl/…`、`surl.amap.com/…` 这类要发一次 HTTP 才展开的
  地址认不出来（跟随重定向等于"粘一条链接就悄悄联网"），需要在输入框里改成**完整的长链接**再确认。
- **附件内容去重只在本次 Obsidian 会话内生效**：重开 Obsidian 后再导入同一张图仍会各存一份。
  跨会话去重要把内容哈希写成一份落盘索引，属于后续范围 —— 但"同一次会话里同一张图拖三次"
  已经只存一份。
- 移动端布局与交互已就绪，但**帧率指标尚未实测**。
- 历史版本的「预览」是**卡片清单**（前 30 张的类型 + 标题/正文），不是画布缩略图：
  快照文件默认不在 Vault 索引里，走不了只读渲染器那条通路。
- **v1.2.0 起提供**：模板市场（本地模板目录）。
  （模板库、PDF / SVG / ZIP 导出、打印、`.canvas` 互转已在**本版**提供，见上面的「新增」。）
- **导出的 SVG 不含图片位图**（图片卡 / 地图卡 / 手绘卡只留文字），**导出的 ZIP 不压缩、也不打包
  笔记与子白板** —— 两者的取舍见上面「新增」里对应的条目。
- **不提供 Milanote 导入向导**（原计划的 `T6.04`，已**取消**）：本插件是"在 Obsidian 里用
  Milanote 的手感"，**不是从 Milanote 搬家的工具**。长期跟一份别人家的导出格式意味着它换一次
  版我们就得跟一次，成本与收益不成比例。真要搬家的话，官方导出物里的图片和文字可以直接拖进
  画布 —— 建卡、归类本来就是这块白板最擅长的事。
- **模板文件也会出现在白板选择器 / 注册表里。** 模板就是合法的 `.nboard`（这正是它"能用别的
  编辑器打开改、能进 Git、能直发给同事"的前提），所以「添加到白板」这类选择器里也能搜到它们。
  把它们放进独立的模板目录（默认 `Templates/`）只是为了"翻文件时一眼分得清"，**不是隐藏**；
  模板目录设成库根时，模板列表会**刻意拒绝所有文件**（"全库的板都是模板"不是任何人想要的）。
